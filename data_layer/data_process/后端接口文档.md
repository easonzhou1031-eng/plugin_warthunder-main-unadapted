# 战雷遥测后端 接口文档

本文档面向前端开发，说明后端服务对外提供的 HTTP 接口、各接口返回的数据结构与字段含义。

> **契约版本：`v1.9`（2026-07-23）**。前端可据此 pin 版本；字段/枚举有破坏性变更时递增次版本号。
> v1.6：新增顶层 `replay` 标志与回放模式降级（回放时仅上报 `replay:true`，停掉全部派生数据）。
> v1.7：新增顶层 `dead` 标志与阵亡待命态（被击杀后→重生前/观战他人期间，抑制告警、置空态势/接近，战绩照常）。
> v1.8：`processed` 新增 `profile_source`/`profile_family`；机型识别加入模糊家族层（未录入的同系列变体按大类套阈值），`profile_matched` 语义扩展为“精确或家族命中均为 true”。
> v1.9：顶层遥测与健康检查新增 `battle_id`、`battle_started_at`、`life_index`、`confirmed_respawns` 和 `dead_source`；同一局内重生保持 `battle_id` 不变并递增 `life_index`，确认离局后下一局生成新 ID。
> 变更历史：
> - `v1.5`：新增 `awards`（战斗嘉奖：一血/双杀/三杀/连续无伤歼敌，含 `is_mine`/`notable`）与 `/api/awards`（见 4.13，实验性）；
>   修复嵌套括号载具解析（`台风 (AESA)` / `◄EF-2000 (AESA)`，惠及击杀流与嘉奖）；
>   服务(重)启/进局时排空 8111 跨局 hud 积压，杜绝上一局事件被错算（假 `you_died`）；嘉奖/掉线/连接播报从击杀流剔除。
> - `v1.4`：`combat.feed.action` 新增伤害日志动词 `lethal_hit`/`severely_damaged`/`set_afire`/`damaged`（非致命，不计 K/D）；单方 `已被摧毁`→`destroyed` 现正确计为阵亡（地面/海军 `you_died`）；掉线/连接等系统噪声从击杀流剔除。新增动作代码表。
> - `v1.3`：`indicators` 增 `weapon_firing`/`game_time`/`game_time_sec`/`gear_state`；`situation` 增 `ground_targets` 且敌我统计按单位类型过滤（`bombing_point` 不再计入敌军）；`processed.flags` 增 `overspeed_warn`/`overspeed_critical`。
> - `v1.2`：`combat` 增自我识别（`self`/`active_players`/涉我标记）、`hud_notices`、`/api/notices`、`/api/identity`。
>
> 所有字段均依据当前源码（`wt_server.py` / `wt_telemetry.py` / `wt_processor.py` / `wt_geo.py` / `wt_proximity.py` / `wt_events.py`）核实。

---

## 一、概述

| 项 | 值 |
|---|---|
| 服务地址 | `http://localhost:8112`（端口可用 `--port` 修改，默认 8112） |
| 数据源 | 游戏本地接口 `http://localhost:8111` |
| 响应格式 | JSON（地图图片接口除外，返回二进制图片） |
| 字符编码 | UTF-8 |
| 跨域 | 默认关闭；确有浏览器前端时，用可重复的 `--cors-origin http://127.0.0.1:端口` 显式放行 |
| 请求方式 | 全部为 `GET`（另支持 `OPTIONS` 预检） |

服务默认只监听本机回环地址，并校验本机 `Host`，避免网页通过 DNS 重绑定读取本地遥测。
会修改状态的昵称设置/清除与录制开关还必须携带请求头
`X-Neko-Warthunder-Action: 1`；不带该请求头时返回 `403 action_header_required`。

### 后端轮询频率（分频多线程）

后端按数据特性分四组、各自独立频率轮询游戏接口并缓存，前端按需取用即可（前端轮询频率与后端解耦）：

| 组 | 默认间隔 | 内容 |
|---|---|---|
| `fast` | 0.1s | `/state`（仪表）+ `/indicators`（座舱）+ 战局状态 |
| `map` | 0.5s | `/map_obj`（地图单位）+ 态势分析 + 接近告警 |
| `events` | 1.0s | `/mission` + `/hudmsg`（击杀/事件）+ `/gamechat`（聊天） |
| `mapimg` | 5.0s | `/map_info`（坐标换算参数）+ 小地图图片 |

各间隔可通过启动参数 `--fast-interval` / `--map-interval` / `--event-interval` / `--mapimg-interval` 调整。

---

## 二、接口一览

| 路径 | 说明 | 返回 |
|---|---|---|
| `/`、`/health`、`/api/health` | 健康检查 | 见 [健康检查](#六健康检查) |
| `/api/telemetry` | **完整快照**（包含以下所有数据） | 见 [完整快照](#三完整快照-apitelemetry) |
| `/api/state` | 载具仪表状态（飞行/航行参数） | `vehicle` 对象 |
| `/api/indicators` | 座舱原始指示数据 | `indicators` 对象 |
| `/api/map_objects` | 地图单位列表 | `MapObject[]` |
| `/api/map_info` | 坐标换算参数 | `map_info` 对象 |
| `/api/processed` | **自定义加工数据 + 告警** | `processed` 对象 |
| `/api/alerts` | 仅告警（精简版） | `{ level, alerts }` |
| `/api/situation` | 态势（最近敌军/距离方位） | `situation` 对象 |
| `/api/proximity` | 敌军接近告警（边沿触发） | `{ thresholds_m, events }` |
| `/api/kills`、`/api/combat` | 战绩（击杀流 / K-D / 自我识别） | `combat` 对象 |
| `/api/identity` | 查看玩家昵称；设置/清除时需动作请求头（`?name=昵称`；`?clear=1`） | `{ requested, self, player_name }` |
| `/api/record` | **（调试）** 查状态；开关录制时需动作请求头：`?on=1` 开 / `?on=0` 关 | `{ recording, session_dir, counts, ... }` |
| `/api/notices` | **自机技术通知**（油温过高/襟翼非对称/发动机过热） | `hud_notices` 对象 |
| `/api/awards` | **战斗嘉奖**（一血/双杀/三杀/连续无伤歼敌；`?notable=1` 仅高光） | `awards` 对象 |
| `/api/hud` | HUD 事件原始流 | `HudMessage[]` |
| `/api/chat` | 聊天消息 | 原始消息数组 |
| `/api/map.jpg`、`/api/map` | 小地图图片 | 二进制图片（无图时 404 JSON） |

未匹配路径返回 `404`：`{ "error": "not found", "path": "..." }`。

---

## 三、完整快照 `/api/telemetry`

一次返回所有数据。结构如下（顶层字段）：

```jsonc
{
  "state": "in_battle",          // 连接状态，见枚举
  "timestamp": 1718800000.12,    // 最近一次 fast 轮询的时间戳（秒）
  "in_battle": true,             // 是否在战局
  "replay": false,               // 是否为录像回放（true 时见下方“回放模式”）
  "dead": false,                 // 是否阵亡待命（被击杀后→重生前/观战他人，见“阵亡待命态”）
  "dead_source": null,           // 阵亡来源：hud_event / ground_crew；未处于阵亡态时为 null
  "battle_id": "...",           // 数据层生成的本地战局 ID；同局重生不变，离局后清空
  "battle_started_at": 1718800000.0, // 本局首次确认进入的时间戳
  "life_index": 2,               // 本局第几次确认出生，首次为 1
  "confirmed_respawns": 1,       // 已确认重生次数，等于 max(life_index - 1, 0)
  "domain": "ground",            // 当前载具域，见枚举
  "domain_label": "陆军",        // 载具域中文

  "vehicle": { ... },            // 载具仪表，见 4.1
  "indicators": { ... },         // 座舱数据，见 4.2
  "map_objects": [ ... ],        // 地图单位，见 4.3
  "map_info": { ... },           // 坐标参数，见 4.4
  "player": { ... } | null,      // 地图上“自己”那个单位（MapObject），无则 null
  "enemy_count": 7,              // 敌方单位数
  "ally_count": 5,               // 友方单位数

  "mission_status": "running" | null,
  "mission_objectives": ... | null,
  "hud_events": [ ... ],         // HUD 事件原始流，见 4.8
  "chat": [ ... ],               // 聊天，见 4.9

  "processed": { ... } | null,   // 加工数据+告警，见 4.5
  "situation": { ... } | null,   // 态势，见 4.6
  "proximity": { ... },          // 接近告警，见 4.7
  "combat": { ... } | null,      // 战绩，见 4.10
  "hud_notices": { ... } | null, // 自机技术通知，见 4.12
  "awards": { ... } | null,       // 战斗嘉奖（一血/连杀），见 4.13
  "meta": { ... }                // 各轮询组运行状态，见 4.11
}
```

### 回放模式（`replay: true`）

当玩家在游戏内观看**战斗录像回放**时，8111 仍报 `in_battle`，但数据语义与实战
完全不同：观战镜头在各载具间切换（载具/速度/油量都不属于单一玩家）、击杀会随
时间轴跳转被**重复上报**、`mission_status` 一进场就是终局结果。这类数据上报给前端
只会制造混乱，因此后端一旦识别为回放（`game_time_sec` 倒退，或进局后 `mission_status`
始终未出现 `running`），**整局降级**：所有接口（含 `/api/telemetry`、`/api/processed`、
`/api/combat`、`/api/situation`、`/api/awards`、`/api/alerts` 等）仅返回回放标记，
**不再上报告警/战绩/态势/嘉奖等任何派生数据**，直到离开战局复位。

回放时 `/api/telemetry` 形如：

```jsonc
{
  "state": "in_battle",
  "timestamp": 1718800000.12,
  "in_battle": true,
  "replay": true,
  "note": "回放模式：当前为战斗录像回放，数据语义不可靠，已暂停上报告警/战绩/态势/嘉奖等",
  "meta": { ... }
}
```

> 前端处理建议：检测到 `replay === true` 即切到“回放模式”UI（隐藏告警/战绩/态势等
> 面板，或显示“正在观看回放”），不要消费此期间任何战斗数据。`/health` 也会带 `replay` 字段，可用于轮询判断。

### 阵亡待命态（`dead: true`）

玩家被击杀后，在**可重生模式重生前**、或选择**转观战他人直到战局结束**期间，8111 的座舱遥测会
冻结在“死车残骸”上（速度 0、坠机后高度不变、乘员减员），若按活载具处理会持续刷失速/低高度/
乘员损失等**假警**；观战他人时地图“自身”坐标还会漂到被观战者身上，令态势（敌距/方位/接近）失真。

后端据此自动识别该状态（`combat.my.deaths` 增加进入；先见载具静止、再恢复运动或乘员满员退出，
即重生/再次起飞行驶），其间：

- `processed.alerts` / `processed.flags` **清空**（数值仍在，但不应据此告警）；
- `situation` 置 `null`、`proximity.events` 置 `[]`（自身位置不可靠）；
- `combat`（战绩 K/D、谁击杀了你）**照常上报**——HUD 带全局名字戳，观战期间他人的击杀**不会**被
  误算到你头上，可放心展示。

`/api/telemetry` 与 `/health` 均带 `dead`、`dead_source`、`battle_id`、`battle_started_at`、`life_index` 和 `confirmed_respawns`。同局阵亡/重生只递增生命序号，不更换战局 ID；只有确认离开战局并再次进入时才生成新 ID。

> 前端处理建议：`dead === true` 时切到“阵亡/观战”UI，停掉飞行告警与态势面板，但可继续展示战绩、
> 谁击杀了你等信息；待 `dead` 转回 `false`（重生）再恢复。与 `replay` 相互独立。

---

## 四、各结构字段详解

### 4.1 `vehicle`（载具仪表，来源 `/state`）

以飞机字段为主，单位见注释。无数据时 `valid=false`、其余为 `null`。

| 字段 | 类型 | 含义 |
|---|---|---|
| `valid` | bool | 数据是否有效 |
| `altitude_m` | float\|null | 海拔高度（米） |
| `tas_kmh` | float\|null | 真空速（km/h） |
| `ias_kmh` | float\|null | 表速（km/h） |
| `mach` | float\|null | 马赫数 |
| `aoa_deg` | float\|null | 迎角（度） |
| `aos_deg` | float\|null | 侧滑角（度） |
| `load_factor` | float\|null | 过载 Ny |
| `climb_ms` | float\|null | 垂直速度（m/s） |
| `roll_rate_dps` | float\|null | 滚转角速度（度/s） |
| `fuel_kg` | float\|null | 当前燃油（kg） |
| `fuel_full_kg` | float\|null | 满油量（kg） |
| `aileron_pct` / `elevator_pct` / `rudder_pct` / `airbrake_pct` / `flaps_pct` / `gear_pct` | float\|null | 各控制面位置（%） |
| `engines` | EngineState[] | 各发动机数据，见下 |
| `raw` | object | 原始 JSON（陆战/其它字段从此取） |

**EngineState**（`engines[]` 元素）：`index`、`throttle_pct`、`power_hp`、`rpm`、`manifold_pressure_atm`、`oil_temp_c`、`thrust_kgs`、`efficiency_pct`。

### 4.2 `indicators`（座舱数据，来源 `/indicators`）

| 字段 | 类型 | 含义 |
|---|---|---|
| `valid` | bool | 是否有效 |
| `army` | string\|null | 兵种：`air` / `tank` 等 |
| `vehicle_type` | string\|null | 载具代号，如 `ki_61_1a_otsu_china` |
| `speed` | float\|null | 速度（m/s） |
| `heading_deg` | float\|null | 罗盘航向 |
| `roll_deg` / `pitch_deg` / `bank_deg` | float\|null | 姿态角 |
| `vario_ms` | float\|null | 升降速率（m/s） |
| `g_meter` / `g_meter_min` / `g_meter_max` | float\|null | 过载表当前/最小/最大 |
| `throttle` | float\|null | 油门（>1 表示 WEP/加力） |
| `flaps` / `gears` / `airbrake` | float\|null | 襟翼/起落架/减速板（连续量 0~1） |
| **开火 / 时间 / 起落架状态** | | |
| `weapon_firing` | bool\|null | 是否正在开火：扫描所有 `weaponN` 扳机键任一触发（槽位随机型不同，实测喷气机用 `weapon2/weapon4`）。**仅空军有此信号**；陆/海战 `indicators` 无 `weaponN` 键，恒为 `null`（不适用，区别于空军“未开火”的 `false`） |
| `game_time` | string\|null | 游戏内时刻 `"HH:MM:SS"`（座舱时钟；无时钟载具为 null） |
| `game_time_sec` | int\|null | 游戏内当日秒数（0~86399） |
| `gear_state` | string\|null | 起落架离散状态：`down`=锁定放下 / `up`=锁定收起 / `moving`=运动中 / null=固定起落架或无此设备 |
| **直升机** | | |
| `is_helicopter` | bool | 是否直升机 |
| `prop_rpm` | float\|null | 旋翼/螺旋桨转速 |
| `radio_altitude` | float\|null | 无线电高度（离地） |
| **发动机类型与温度** | | |
| `is_jet` | bool | 是否喷气发动机 |
| `water_temperature` | float\|null | 水温℃（仅液冷活塞机） |
| `head_temperature` | float\|null | 缸头温度℃（仅气冷活塞机） |
| `turbine_temperature` | float\|null | 涡轮/排气温度℃（仅喷气机） |
| `oil_temperature` | float\|null | 油温℃ |
| **地面载具（坦克）** | | |
| `gear` | float\|null | 档位 |
| `gear_neutral` | float\|null | 空挡档位编号 |
| `rpm` | float\|null | 发动机转速 |
| `stabilizer` | float\|null | 火炮稳定器（1=启用） |
| `first_stage_ammo` | float\|null | 一级待发弹数 |
| `crew_total` / `crew_current` | float\|null | 乘员总数/存活数 |
| `cruise_control` | float\|null | 定速巡航档 |
| `lws` | float\|null | 激光告警：`-1`=无设备、`0`=待机、`1`=正在告警、`2`=设备损坏 |
| `ircm` | float\|null | 红外对抗 |
| `has_speed_warning` | float\|null | 超速/转向告警 |
| `gunner_state` / `driver_state` | float\|null | 炮手/驾驶员状态 |
| `raw` | object | 原始 JSON |

### 4.3 `map_objects`（地图单位，`MapObject[]`）

坐标均为 0~1 归一化值（x 向右=东，y 向下=南）。

| 字段 | 类型 | 含义 |
|---|---|---|
| `type` | string | 大类：`aircraft` / `ground_model`（含坦克与军舰）/ `airfield` 等 |
| `icon` | string | 图标/兵种：`Player` / `Fighter` / `MediumTank` / `SPAA` 等 |
| `faction` | string | 阵营：`self` / `ally` / `enemy` / `other`（按颜色推断） |
| `color_hex` | string | 颜色 `#RRGGBB` |
| `color_rgb` | [int,int,int] | RGB |
| `blink` | int | 闪烁标志 |
| `x` / `y` | float\|null | 点坐标（归一化） |
| `dx` / `dy` | float\|null | 朝向单位向量（飞机才有） |
| `sx` / `sy` / `ex` / `ey` | float\|null | 线段端点（机场跑道等） |

### 4.4 `map_info`（坐标换算参数，来源 `/map_info`）

| 字段 | 类型 | 含义 |
|---|---|---|
| `valid` | bool | 是否有效 |
| `map_generation` | int\|null | 地图版本号（换图递增） |
| `grid_size` | [float,float]\|null | 网格参考区域尺寸（**注意：不是整图尺寸**） |
| `grid_steps` | [float,float]\|null | 网格格距（米） |
| `grid_zero` | [float,float]\|null | 网格原点（米） |
| `map_min` / `map_max` | [float,float]\|null | 地图世界边界（米）。**真实距离换算用 `map_max - map_min`** |
| `raw` | object | 原始 JSON |

### 4.5 `processed`（加工数据 + 告警，核心）

后端基于机型配置（`vehicle_profiles.json`）计算的派生信息与告警。

> **机型识别三级（模糊家族识别）**：收到 `vehicle_type` 后依次尝试——①在配置表精确命中
> （`profile_source=exact`）；②未命中且为空军时，按“厂商+型号”家族前缀做模糊识别
> （`profile_source=family`，例如 `fw-190a-9`、`bf-109g-14` 等未单独录入的亚型会归到 `Fw 190`/`Bf 109`
> 家族，套用该家族大类阈值）；③仍不中才回退默认值（`profile_source=default`）。同系列变体的失速/超速/
> 高度等阈值差异不大，按家族大类处理即可。前端若需展示“识别到的机型/家族”，`profile_source=family`
> 时用 `profile_family`，`exact` 时用 `vehicle_type`。

> **数据库维护来源**：exact profile 键名优先采用 War Thunder Wiki `/unit/<gameId>` slug / 页面 `gameId`，并以游戏 `/indicators` 暴露的 `vehicle_type` 实测校验；没有 Wiki/实测确认时才用 Datamine id 尽力填充。
> `tools/vehicle_profile_id_audit.py` 会离线检查 key 形态、实机确认 id、已复核 alias 和运行副本同步口径；`vehicle_profile_vetted_ids.json` 只记录已实机确认的 `vehicle_type`，`vehicle_profile_identity_aliases.json` 只记录已复核但暂不删除的 alias。
> `tools/datamine_profile_candidates.py` 可从 gszabi99/War-Thunder-Datamine 抽取只读候选报告，`vehicle_profiles.json` 已从 Datamine flightmodels 维护飞模候选，并从 `char.vromfs.bin_u/config/wpcost.blkx` 回填小型 vehicle profile economy metadata（`rank` / `economic_rank_*` / `country` / `unit_class` / `unit_move_type`）；`economicRank*` 仅作为经济元数据保存，不宣称等同 UI BR。通用参数仍按飞模性能段 class 维护（例如 transonic / early-supersonic / subsonic attacker / high-alpha modern / heavy modern），exact 字段仍优先。`tools/update_vehicle_profiles_from_datamine.py` 可从本地 Datamine checkout 批量补齐 fixed-wing exact profile，`tools/update_vehicle_profile_economy_from_datamine.py` 可从 `wpcost.blkx` 回填小型经济/权重元数据，
> `tools/profile_candidate_diff.py` 可把候选报告与当前 `vehicle_profiles.json` 做差异复核。
> `stallSpeed`、`alphaCritHigh`、`AoaLimits`、`VNE/MNE` 可用于复核 `vehicle_profiles.json`；FM `Temperature`
> 的 `Load*` 水温/油温可作为 exact/family 温度阈值候选，但不得放进 `_default` 泛化到所有载具。
> FM `Mass.*`、`Aerodynamics.WingPlane.Strength.CritOverload`、`Mass.WingCritOverload`、`Instructor.loadFactorLimit`、
> `EngineType*.Main.FuelConsumption*` 和 `EngineType*.Main.EngineInertiaMoment` 已可作为只读维护证据入库。
> 其中 `CritOverload` / `WingCritOverload` 是结构力证据，不是可直接播报的 G 阈值；当前数据库只保存候选 G 计算结果。
> 高 G detector 在存在 `Instructor.loadFactorLimit` 时优先使用飞控/Instructor 限制作为播报阈值，结构候选只作缺省回退。
> `overheatBlk` 代表过热时间/冷却模型，不是单帧温度阈值。Datamine/Wiki 候选值在真机样本验证前不得标为 `_tested`。

| 字段 | 类型 | 含义 |
|---|---|---|
| `vehicle_type` | string\|null | 载具代号 |
| `profile_matched` | bool | 是否套用到了机型适配阈值（精确或家族模糊命中均为 `true`；`false`=回退默认值） |
| `profile_source` | string | 阈值来源：`exact`=精确命中机型 / `family`=模糊家族命中（同系列变体按大类套用） / `default`=回退默认 |
| `profile_family` | string\|null | 模糊命中的家族标签（如 `"Fw 190"`、`"F-4 Phantom II"`），仅 `profile_source=family` 时有值 |
| `army` | string\|null | 兵种 |
| `vehicle_class` | string | 归类：`air` / `ground` / `heli` / `unknown` |
| `fuel_fraction` | float\|null | 剩余燃油比例 0~1 |
| `fuel_kg` | float\|null | 当前燃油 |
| `fuel_burn_rate_kgs` | float\|null | 估算油耗（kg/s） |
| `fuel_remaining_sec` | float\|null | 估算剩余可飞时间（秒） |
| `afterburner_active` | bool | 是否开加力 |
| `afterburner_elapsed_sec` | float | 连续加力时长（秒） |
| `afterburner_max_sec` | float\|null | 加力时间上限（0/null=不限） |
| `ias_kmh` / `aoa_deg` / `altitude_m` | float\|null | 表速/迎角/高度 |
| `g_now` / `g_max` / `g_min` | float\|null | 当前/本架次峰值/谷值过载 |
| `crew_current` / `crew_total` | float\|null | 坦克乘员存活/总数 |
| `ammo_first_stage` | float\|null | 一级待发弹 |
| `gun_stabilizer` | bool\|null | 火炮稳定器 |
| `gear_position` | int\|null | 档位：>0 前进/0 空挡/<0 倒车 |
| `gunner_state` / `driver_state` | float\|null | 炮手/驾驶员原始状态：`0`=正常、`1`=无人补位、`2`=正在补位；`3` 的语义未确认，只透传不派生告警 |
| `rotor_rpm` | float\|null | 旋翼转速（直升机） |
| `radio_altitude_m` | float\|null | 离地高度（AGL；来自 `/indicators.radio_altitude`，8111 提供时固定翼/直升机均可用） |
| `vario_ms` | float\|null | 升降速率（直升机） |
| `water_temp_c` | float\|null | 水温（液冷活塞） |
| `head_temp_c` | float\|null | 缸头温度（气冷活塞） |
| `turbine_temp_c` | float\|null | 涡轮温度（喷气） |
| `oil_temp_c` | float\|null | 油温 |
| `alerts` | Alert[] | 告警列表，见下 |
| `flags` | object | 各告警代码 -> 是否触发（bool 映射） |
| `level` | string | 所有告警的最高等级：`info` / `warning` / `critical` |

**Alert**（`alerts[]` 元素）：

| 字段 | 类型 | 含义 |
|---|---|---|
| `code` | string | 告警代码，见下表 |
| `level` | string | `info` / `warning` / `critical` |
| `message` | string | 中文提示文本 |
| `value` | float\|null | 相关数值 |

**告警代码（`code`）**：

| code | level | 含义 |
|---|---|---|
| `fuel_low` / `fuel_critical` | warning / critical | 燃油偏低 / 告急 |
| `afterburner_warning` / `afterburner_overrun` | warning / critical | 加力将达上限 / 加力超时 |
| `stall_warning` / `stall_critical` | warning / critical | 失速 |
| `overspeed_warn` / `overspeed_critical` | warning / critical | 超速（IAS 超结构限速，或喷气机马赫超压缩限制；放起落架时不抑制） |
| `aoa_high` / `aoa_critical` | warning / critical | 迎角过大 |
| `over_g` / `over_g_critical` | warning / critical | 过载过大（正/负过载；优先按 profile 的 Instructor G 限制判断，缺少时回退结构过载候选） |
| `altitude_low` / `altitude_critical` | warning / critical | 高度过低 |
| `engine_overheat` / `engine_overheat_critical` | warning / critical | 发动机过热（水温/缸头/涡轮，消息文本标明） |
| `oil_overheat` / `oil_overheat_critical` | warning / critical | 机油过热 |
| `vrs_warning` / `vrs_critical` | warning / critical | 直升机涡环状态 |
| `crew_loss` / `crew_critical` | warning / critical | 坦克乘员阵亡 / 濒临阵亡 |
| `gunner_disabled` / `driver_disabled` | warning | 岗位无人补位或正在补位（仅状态 `1/2` 触发；`0/3` 不触发） |
| `ammo_low` / `ammo_empty` | info / warning | 一级弹药偏少 / 耗尽；负数为不可用，必须先建立正数基线，再从正数降到 `0` 才触发耗尽 |
| `laser_warning` | critical | 遭激光照射（可能被锁定/测距）；仅 `lws == 1` 触发 |

> 告警为**状态型**：只要条件持续满足，每帧都会出现在 `alerts` 中，前端可自行去重展示。

> **2026-07-10 陆战修复**：`crew_current/crew_total` 继续作为乘员损失事实；岗位状态按多值枚举处理，LWS 仅在状态 `1` 告警，一级弹药增加正数基线与同局重生重置。状态 `3` 仍只保留原值，等待更多真机样本。

### 4.6 `situation`（态势，来源态势分析）

以“自己”为中心，输出敌军距离方位。距离已修正为真实米值。

| 字段 | 类型 | 含义 |
|---|---|---|
| `has_player` | bool | 是否找到自己 |
| `enemy_count` / `ally_count` | int | 敌/友**作战单位**数量（仅 `aircraft`/`ground_model`；`bombing_point` 等任务标记不计入） |
| `player` | object\|null | `{ x, y, heading_deg, grid }` |
| `nearest_enemy` | object\|null | 最近敌军（结构同 `enemies[]`） |
| `nearest_air_threat` | object\|null | 最近敌机 |
| `air_threat_count` | int | 敌机数量 |
| `enemies` | array | 全部敌军，按距离升序 |
| `ground_targets` | array | **对地任务目标点**（轰炸点等），按距离升序；未找到自己时仅含坐标 |

> 注：`bombing_point`（轰炸点）在游戏里是红色图标，旧逻辑会按颜色误判成 `enemy`。现已按类型过滤——
> 它**不再计入** `enemy_count`/`enemies[]`，而是单独归入 `ground_targets`。

**`enemies[]` 元素**：

| 字段 | 类型 | 含义 |
|---|---|---|
| `icon` | string | 图标 |
| `type` | string | 大类 |
| `category` | string\|null | **中文兵种类别**（见枚举） |
| `x` / `y` | float | 归一化坐标 |
| `distance_m` | int\|null | 距离（米） |
| `bearing_deg` | float | 绝对方位角（正北=0，顺时针） |
| `relative_deg` | float\|null | 相对自身航向（-180~180，负左正右） |
| `heading_deg` | float\|null | 该单位航向（飞机才有） |

**`ground_targets[]` 元素**（对地任务目标点）：

| 字段 | 类型 | 含义 |
|---|---|---|
| `kind` | string | 目标类型，当前为 `bombing_point` |
| `label` | string\|null | 中文标签，如 `轰炸点` |
| `x` / `y` | float | 归一化坐标 |
| `grid` | string\|null | 网格标签（如 `B4`） |
| `distance_m` | int\|null | 距自己的距离（米，未找到自己时为 null） |
| `bearing_deg` | float\|null | 绝对方位角（正北=0，顺时针） |
| `relative_deg` | float\|null | 相对自身航向（-180~180，负左正右） |

### 4.7 `proximity`（敌军接近告警，边沿触发）

```jsonc
{
  "thresholds_m": { "vs_air": 3000, "vs_ground": null },  // 当前生效的告警距离；null=该类不告警
  "events": [ ... ]   // 触发的接近事件流（累积，上限 100 条）
}
```

**接近事件（`events[]` 元素）**：敌军**首次进入**告警距离时触发一次（持续在范围内不重复）。

| 字段 | 类型 | 含义 |
|---|---|---|
| `id` | int | 事件序号（递增） |
| `ts` | float | 时间戳（秒） |
| `kind` | string | `enter`=从范围外进入 / `appear`=范围内新出现 |
| `icon` | string | 图标 |
| `type` | string | 大类 |
| `category` | string\|null | 中文兵种类别 |
| `is_air` | bool | 是否空中目标 |
| `distance_m` | int\|null | 进入时距离（米） |
| `bearing_deg` | float\|null | 绝对方位角 |
| `compass` | string\|null | 八方位中文（北/东北/…） |
| `relative_deg` | float\|null | 相对航向角 |
| `clock` | int\|null | 时钟方位（12=正前，3=右，6=后，9=左） |
| `threshold_m` | int\|null | 触发时所用的距离阈值 |

> 告警距离随**我方兵种 × 敌方类型**变化：空军/直升机仅对空中敌人告警；陆军/海军仅对地面/水面敌人告警。详见 `vehicle_profiles.json` 的 `_proximity`。

### 4.8 `hud_events`（HUD 事件，`HudMessage[]`）

| 字段 | 类型 | 含义 |
|---|---|---|
| `id` | int | 事件 id |
| `kind` | string | `event` / `damage` |
| `msg` | string | 文本 |
| `sender` | string | 发出方 |
| `enemy` | bool | 是否敌方 |
| `mode` | string | 模式 |
| `time` | int\|null | 游戏内时间 |

### 4.9 `chat`（聊天）

游戏 `/gamechat` 原始消息数组，每条含 `id`、`msg`、`sender`、`mode`、`enemy` 等原始字段（增量推送）。

### 4.10 `combat`（战绩，来源击杀解析）

| 字段 | 类型 | 含义 |
|---|---|---|
| `active_players` | array | **高活跃度玩家列表**（按 击杀+阵亡 降序），见下 |
| `self` | object | 自我识别（谁是“自己”），见下 |
| `player_name` | string\|null | 已确定的“自己”昵称（= `self.name`；未确定时 null） |
| `total_events` | int | 已解析事件总数 |
| `by_action` | object | 各动作计数（动作代码 -> 次数） |
| `leaderboard` | array | 真人玩家榜 `{ name, kills, deaths }`，按击杀数排序，前 20（已排除 AI 地面单位） |
| `my` | object\|null | 我的战绩 `{ kills, deaths }`（识别出自己后自动给出，含历史回填） |
| `feed` | KillEvent[] | 击杀流（最新在前） |

**`active_players`（高活跃度玩家，元素）**：直接上报前端，**不区分敌我**（API 拿不到其他玩家队伍）；前端可据此展示/让用户点选自己。前 30。

| 字段 | 类型 | 含义 |
|---|---|---|
| `name` | string | 玩家名（已排除 AI 地面单位/AI 空军） |
| `kills` / `deaths` | int | 本局击杀 / 阵亡数 |
| `score` | int | 活跃度 = `kills + deaths`（排序依据） |
| `vehicle` | string\|null | 最常用的本地化载具名（如 `三式战一型`） |

**`self`（自我识别）**：

| 字段 | 类型 | 含义 |
|---|---|---|
| `name` | string\|null | 判定的“自己”昵称；自动模式置信度不足时为 null |
| `source` | string | `manual`（前端/启动参数设的昵称，权威，置信度 1.0）/ `auto`（按活跃度投票推断） |
| `confidence` | float | 0~1 置信度；`auto` 上限 0.85（永不宣称 100%），≥0.5 才用于统计 |

> **自我识别说明**：游戏遥测不暴露玩家昵称、也无可靠“阵亡”信号（实测 `indicators.valid` 全程为真）。
> 推荐前端从 `active_players` 让用户点选自己，并通过带
> `X-Neko-Warthunder-Action: 1` 请求头的 `GET /api/identity?name=昵称` 设为权威昵称；
> 未设置时，`self` 会按活跃度给出一个 best-effort 猜测（`auto`，置信度不足时 `name=null`）。

**KillEvent**（`feed[]` 元素）：

| 字段 | 类型 | 含义 |
|---|---|---|
| `id` | int | 事件 id |
| `time` | int\|null | 游戏内时间 |
| `action` | string | 动作代码（见下表） |
| `action_label` | string | 中文动作标签 |
| `killer` / `killer_squad` / `killer_vehicle` | string | 施动者 名/战队/载具 |
| `killer_is_ai` | bool | 施动者是否 AI |
| `victim` / `victim_squad` / `victim_vehicle` | string | 受动者 名/战队/载具 |
| `victim_is_ai` | bool | 受动者是否 AI |
| `is_kill` | bool | 是否致命事件 |
| `involves_me` | bool | 本事件是否涉及“自己”（识别出自己后才可能为 true） |
| `is_my_kill` | bool | 是否“我”的击杀 |
| `is_my_death` | bool | 是否“我”的阵亡 |
| `parsed` | bool | 是否成功解析 |
| `raw` | string | 清洗后的原文 |

**动作代码（`action`）**：分两类，`is_kill` 标明是否计入 K/D。

| 代码 | 标签 | `is_kill` | 来源/实测措辞 |
|---|---|---|---|
| `shot_down` | 击落 | ✓ | 击杀榜：`X 击落了 Y`（空战互杀） |
| `destroyed` | 击毁 | ✓ | 击杀榜：`X 击毁了 Y` / 单方 `X 已被摧毁`（地面/海军阵亡） |
| `crashed` | 坠毁 | ✓ | 单方：`X 已坠毁。`（飞机坠机/被击落后坠地） |
| `wrecked` | 损毁 | ✓ | 单方：`X 已失控` |
| `lethal_hit` | 致命一击 | ✗ | 伤害日志：`X 致命攻击 Y`（细粒度，常打 AI bot，**不计 K/D** 以免与击杀榜重复） |
| `severely_damaged` | 重创 | ✗ | 伤害日志：`X 重创 Y` |
| `set_afire` | 点燃 | ✗ | 伤害日志：`X 的攻击引燃了 Y` |
| `damaged` | 击伤 | ✗ | 伤害日志：`X 击伤 Y` |

> 同属 `kind=='damage'` 的两类文本在一局里并存：**击杀榜**（致命，权威 K/D）与**伤害日志**（细粒度，非致命）。
> 系统噪声（`X 已掉线。` / `NET_PLAYER_DISCONNECT_FROM_GAME` / 连接/加入）已在数据层剔除，不会出现在 `feed`。

> **击杀流的敌我标注**：游戏 API 不暴露其他玩家的队伍（hudmsg `enemy` 字段实测恒为 false），
> 无法可靠给击杀流每个名字标敌/友；故仅在识别出“自己”后标注 `involves_me/is_my_kill/is_my_death`。
> 地图单位的敌我识别请用 `map_objects[].faction` / `situation`（按队伍颜色，可靠）。

#### 事件消费建议（`you_killed` / `you_died`）

> 针对插件侧 `you_killed`/`you_died` 事件，数据层给出的可靠消费方式（基于真机抓包验证）：
>
> - **`you_killed`**：监听 `feed[]` 中 `is_my_kill == true` 的**新 id**（按 id 去重；多杀可在窗口内合并）。
>   击杀文本如 `我 (载具) 击毁了 敌`。
> - **`you_died`**：监听 `feed[]` 中 `is_my_death == true` 的**新 id**。**这是唯一可靠的死亡信号**。
>   实测覆盖三种措辞，均正确触发 `is_my_death`：
>   - 被真人击落（双方形式）：`敌 (苏-30SM2) 击落了 我 (歼-15T)` —— `action=shot_down`；
>   - 自身坠机（单方形式）：`我 (歼-15T) 已坠毁。` —— `action=crashed`；
>   - 地面/海军被摧毁（单方形式）：`我 (哈立德 I) 已被摧毁` —— `action=destroyed`。
>   注意被击落前常先有一条伤害日志 `敌 致命攻击 我 (...)`（`action=lethal_hit`，非致命，**不触发** `is_my_death`），
>   真正的死亡以随后的 `击落了`/`已坠毁`/`已被摧毁` 为准。
> - **不要用 `valid` 翻转判死亡**：实测整局（1181 帧）`/api/state.valid` 与 `/api/indicators.valid`
>   **全程为 true，从不翻转**，无法用于判定阵亡/退场；`mission_status` 仅用于战斗结束（`battle_end`）。
> - 前提：`you_killed`/`you_died` 都依赖已确定“自己”（`combat.self.name` 非 null）。未识别出自己时这两个标记恒为 false，
>   请先经 `/api/identity` 设定或等待 `auto` 置信度达标。

### 4.11 `meta`（轮询组运行状态）

每组（`fast` / `map` / `events` / `mapimg`）一条：

| 字段 | 类型 | 含义 |
|---|---|---|
| `interval` | float | 配置的轮询间隔（秒） |
| `count` | int | 已轮询次数 |
| `age_sec` | float\|null | 距上次更新的秒数（null=尚未更新） |

### 4.12 `hud_notices`（自机技术通知，来源 `/hudmsg` 文本解析）

从 HUD 文本流（`/hudmsg`）里解析出的、**关于“自己载具”**的技术状态通知。

与 `combat`（击杀流，“击杀者→被击杀者”结构）区分：本字段只收无施动者、描述自身
状态的警告（如油温过高、左右襟翼非对称展开、发动机过热）。这类消息天然只关于自己——
游戏不会告诉你敌人的油温/襟翼，因此不会与击杀流的双方动词冲突。

> 解析采用**关键词宽松匹配**（中英文客户端均覆盖），措辞随游戏版本/语言略有差异时仍可命中；
> 通知按 `(kind, id)` 去重，**新出现一条就推送一条**（不是状态型，不会每帧重复）。

| 字段 | 类型 | 含义 |
|---|---|---|
| `total` | int | 本局累计解析出的通知总数 |
| `by_code` | object | 各通知代码 -> 次数 |
| `feed` | HudNotice[] | 通知流（最新在前，上限 100 条） |

**HudNotice**（`feed[]` 元素）：

| 字段 | 类型 | 含义 |
|---|---|---|
| `id` | int | 来源 HUD 消息 id |
| `time` | int\|null | 游戏内时间 |
| `code` | string | 通知代码，见下表 |
| `level` | string | `info` / `warning` / `critical` |
| `label` | string | 中文标签 |
| `message` | string | 清洗后的原文 |
| `raw` | string | 清洗后的原文（同 `message`，便于对照） |

**通知代码（`code`）**：

| code | level | label | 触发关键词（任一组全部命中即归类） |
|---|---|---|---|
| `oil_overheat` | warning | 油温过高 | 油温+过高 / 油温+过热 / 机油+过热 / oil+overheat |
| `flap_asymmetric` | warning | 襟翼非对称展开 | 襟翼+非对称 / 襟翼+不对称 / 襟翼+不一致 / flap+asymmetr |
| `engine_overheat` | warning | 发动机过热 | 发动机+过热 / 水温+过高 / engine+overheat / water+overheat |
| `powertrain_failure` | critical | 动力系统故障 | 动力系统故障 / 螺旋桨损坏 / 发动机损坏 / engine+disabled |

> 关键词表为**数据驱动**（`wt_events.py` 的 `_SELF_NOTICES`），后续按真机抓包结果可直接增补/校准。
> 已用真机抓包校准（液冷机 Ki-61）：实测命中 `左右襟翼非对称展开`、`水温过高`（液冷机报水温而非油温，归 `engine_overheat`）、`动力系统故障：螺旋桨损坏`。
> `clean_text` 会剥离零宽字符与控制图符区（U+2400–U+243F，游戏在载具名前插的占位字形）。

### 4.13 `awards`（战斗嘉奖，来源 `/hudmsg` 文本解析）｜实验性

从 HUD 文本流解析出的**战斗嘉奖/连杀播报**，天然带玩家名+战队+载具，可用于做高光提示，
或识别敌方高威胁玩家（连杀王）。实测格式：
`<[战队] 玩家名> (载具) 获得嘉奖“双杀！”`，一血为独立句式 `<玩家名> (载具) 先拔头筹！`。

> 与 `combat`（击杀流）区分：嘉奖无“击杀者→被击杀者”结构，已从击杀流剔除、单独成流。
> 按 `(kind, id)` 去重，新出现一条推一条。对应接口 `GET /api/awards`（`?notable=1` 仅返回高光子集）。

| 字段 | 类型 | 含义 |
|---|---|---|
| `total` | int | 本局累计嘉奖数 |
| `by_code` | object | 各嘉奖代码 -> 次数 |
| `feed` | Award[] | 全部已识别嘉奖（最新在前，上限 60） |
| `notable` | Award[] | **罕见而重要**的子集（一血/连杀/连续无伤歼敌等），建议前端只展示/推送这个 |

**Award**（`feed[]` / `notable[]` 元素）：

| 字段 | 类型 | 含义 |
|---|---|---|
| `id` | int | 来源 HUD 消息 id |
| `time` | int\|null | 游戏内时间 |
| `player` / `squad` / `vehicle` | string | 获奖者 名/战队/载具 |
| `is_ai` | bool | 是否 AI |
| `code` | string | 嘉奖代码，见下表（未知嘉奖为 `award_other`） |
| `label` | string | 嘉奖原始名称（如 `连续无伤歼敌×3`） |
| `tier` | string | `major`（高光：一血/连杀）/ `notable` / `minor` |
| `notable` | bool | 是否罕见重要（决定是否进 `notable` 子集） |
| `is_mine` | bool | 是否“自己”获得（依赖已识别 `combat.self`） |
| `raw` | string | 清洗后的原文 |

**嘉奖代码（`code`）**（已实测：first_blood / final_blow / double_kill / triple_kill / killing_spree / air_killer / fighter_savior / comrade / unscathed）：

| code | tier | 触发关键词 | 说明 |
|---|---|---|---|
| `first_blood` | major | 先拔头筹 | 全场首杀（独立句式，无“获得嘉奖”包裹） |
| `final_blow` | major | 最后一击 | “完成了最后一击！”绝杀（独立句式） |
| `double_kill` / `triple_kill` / `quad_kill` / `penta_kill` | notable/major | 双杀/三杀/四杀/五杀 | 短时间连续击杀 |
| `killing_spree` | major | 连续无伤歼敌 | 不掉血连续歼敌（label 带 ×N） |
| `air_killer` | notable | 空中杀手 | 空战多杀 |
| `fighter_savior` | notable | 战斗机救星 | 击落正在攻击队友的敌机 |
| `comrade` | minor | 同仇敌忾 | 团队协作类，`notable=false` 默认不推 |
| `unscathed` | minor | 毫发无伤 | 无伤歼敌（常见，默认不推） |
| `multi_kill` / `terminator` / `battle_hero` / `air_dominance` / `sky_king` | major/notable | 多杀/终结者/战场英雄/制空/空中之王 | 预置词条，未实测 |
| `award_other` | minor | （兜底） | 未在词表中的新嘉奖，仍解析以便发现 |

> 词表为**数据驱动**（`wt_events.py` 的 `_AWARD_RULES`），可按真机抓包增补。多杀/终结者等暂为预置、待实测校准。

---

## 五、枚举值参考

### 连接状态 `state`

| 值 | 含义 |
|---|---|
| `offline` | 端口连不上（游戏没开 / 8111 未监听） |
| `not_in_battle` | 能连上但无效（机库/菜单/加载中） |
| `in_battle` | 已进入战局，数据有效 |

### 载具域 `domain` / `domain_label`

| `domain` | `domain_label` |
|---|---|
| `menu` | 主界面/机库 |
| `air` | 空军 |
| `heli` | 直升机 |
| `ground` | 陆军 |
| `naval` | 海军 |
| `unknown` | 未知 |

### 兵种中文类别 `category`

| icon | category |
|---|---|
| `LightTank` / `MediumTank` / `HeavyTank` | 坦克 |
| `TankDestroyer` / `SPG` | 反坦克车 |
| `SPAA` | 防空车 |
| `Wheeled` | 轮式车辆 |
| `Truck` | 运输车 |
| `Structure` | 建筑/工事 |
| 其它地面单位 | 未知地面载具 |
| `Fighter` | 战斗机 |
| `Bomber` | 轰炸机 |
| `Assault` / `Attacker` | 攻击机 |
| 其它飞机 | 未知飞机 |
| 机场/占领区等场景物 | `null` |

### 告警等级 `level`

`info`（无告警）< `warning`（警告）< `critical`（危急）。

---

## 六、健康检查

`GET /`、`/health`、`/api/health`：

```jsonc
{
  "ok": true,
  "service": "wt-telemetry",
  "state": "in_battle",          // 连接状态
  "replay": false,               // 是否为录像回放（true 时降级，见“回放模式”）
  "dead": false,                 // 是否阵亡待命（true 时抑制告警/态势，见“阵亡待命态”）
  "dead_source": null,           // 阵亡来源；正常存活时为 null
  "battle_id": "...",           // 当前本地战局 ID；不在战局时为 null
  "battle_started_at": 1718800000.0,
  "life_index": 2,
  "confirmed_respawns": 1,
  "updated_at": 1718800000.12,   // 最近 fast 时间戳
  "has_map": true,               // 是否已缓存小地图
  "map_generation": 2,           // 地图版本号
  "groups": { ... }              // 同 meta，各轮询组状态
}
```

---

## 七、重要说明与已知局限

1. **敌人型号不可知**：游戏只暴露敌人的**兵种类别**（`icon` / `category`），不提供具体型号；具体型号仅自己的载具有（`indicators.vehicle_type`）。
2. **坦克与军舰同为 `ground_model`**：`type` 无法区分二者，需结合 `domain` 判断。
3. **海军无仪表数据**：海军模式下 `/indicators`、`/state` 多为无效，`vehicle`/`indicators` 字段大多为 `null`，但地图/态势/接近告警仍可用。
4. **距离换算**：基于 `map_max - map_min`；若地图无该参数则回退 `grid_size`（可能不准）。
5. **对战类型不可知**：游戏接口不提供街机/真实/全真（AB/RB/SB）信息。
6. **温度告警按发动机类型自动分流**：液冷看水温、气冷看油温+缸头、喷气看涡轮温度；不适用者字段为 `null` 且不告警。

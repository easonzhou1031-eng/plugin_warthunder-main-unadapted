# 待办事项

## 已完成

- [x] **模糊机型家族识别（契约 v1.8）**（2026-06-21）
- [x] **稳定战局/生命序号（契约 v1.9）**（2026-07-23）：同局重生保持 `battle_id`，递增 `life_index`；确认离局后清空并在下一局生成新 ID，供插件可靠清理跨局状态。
  - **需求**：机型变体众多难以逐一录入，但同家族（Fw 190 / Bf 109 各亚型）告警阈值差异不大，可按大类处理。
    收到 `vehicle_type` 后若精确表未命中，在回退默认前先做一次家族模糊识别。
  - **实现**（`wt_processor`）：匹配三级——精确(`profile_source=exact`) → 家族模糊(`family`，仅空军) → 默认(`default`)。
    家族表 `_families` 写在 `vehicle_profiles.json`（283 条，`{prefix,label,class,可选覆盖}`）。匹配：`vehicle_type`
    去分隔符转小写后与各家族 `prefix`(同样 compact) 做“开头匹配”，**最长前缀优先**（加载期按长度降序，作者排序无关）。
  - **消歧（最长优先是关键）**：正确区分 `f4u`(海盗/螺旋桨)↔`f4`(鬼怪/现代喷气)、`ki100`↔`ki10`(双翼)、
    `yak15`(喷气)↔`yak1`(螺旋桨)、`i153`↔`i15`、`a7m`(烈风/螺旋桨)↔`a7`(攻击机)、`a10`↔`a4`、`tu22`(喷气轰)↔`tu2`(螺旋桨轰)。
  - **低误判设计**：①仅空军启用（家族阈值都是固定翼类别，`army!=air` 直接跳过，地/海载具不受影响）；
    ②`prefix` 用完整“厂商+型号数字”避免误吞邻近型号；③精确命中永远优先，已录机型不受影响。
  - **新字段**：`processed.profile_source`(exact/family/default)、`profile_family`(家族标签)；`profile_matched`
    扩展为“精确或家族命中均 true”。前端展示用 family 时取 `profile_family`、exact 时取 `vehicle_type`。
  - **验证**：34 项离线用例全过（精确命中、未知变体归类、全部嵌套消歧、地面跳过、彻底未知回退默认）。
  - **2026-07-02 维护复核**：按 `vehicle_profiles` README / 接口文档 / `wt_processor` 合并规则复核后，补齐一批常见机系 family，并给热门喷气 family/exact 条目补入 Wiki IAS/Mach 候选超速阈值；精确条目仍优先于 family，`j_15t` 保留真机断翼采样校准基线，未把 Wiki 候选值标为 `_tested`。
  - **2026-07-02 Datamine 候选抽取/差异报告**：新增只读工具 `tools/datamine_profile_candidates.py`，默认输出到已忽略的
    `local_test_logs/datamine_profile_candidates.json`。可从 gszabi99/War-Thunder-Datamine 的 FM JSON 抽取
    `Passport.Alt.stallSpeed`、`Aerodynamics.WingPlane.FlapsPolar*.alphaCritHigh`、
    `Autopilot.Pitch.AoaLimits`、`Aerodynamics.WingPlane.Strength.VNE/MNE` 等候选字段；同时报告
    `EngineType*.Temperature` 和 `overheatBlk`。`tools/profile_candidate_diff.py` 负责对比当前 `vehicle_profiles.json`
    与候选报告，只产出维护差异，不自动写库。维护规则：失速/迎角/超速可作为候选复核；FM `Temperature.Load*`
    可按 exact/family 小批量补温度阈值，但不得放入 `_default` 泛化到所有载具；`overheatBlk` 只代表过热/冷却时间模型。
    所有 Datamine 候选值在真机样本校准前不得标为 `_tested`。
    本批已导入 J-15T/Su-30/F-16/F/A-18/Mirage/Mirage 2000/A6M 的失速、迎角或 VNE/MNE 候选；`j_15t`
    的 IAS 超速仍保留真机断翼基线，仅补 Datamine Mach 候选。
  - **2026-07-02 Datamine 第二批**：按同一报告链路补入 F-14/F-15/MiG-29/Su-27/Su-33/J-10/J-11/Rafale/JAS39/Eurofighter
    的失速与迎角候选；F-15E 的 Mach 上限高于 F-15C/J family，单独新增 `f_15e` exact，避免把全 F-15 family 错提到 E 型候选。
    `local_test_logs/profile_candidate_diff_batch2.json` 显示 18/18 当前条目匹配候选；报告文件仍不入库。
  - **2026-07-02 Datamine 第三批**：补入 F-5/MiG-21/MiG-23/F-104/A-10/Su-25/Tornado/Kfir/Harrier/AV-8/Sea Harrier/Yak-141
    的失速、迎角、VNE/MNE 候选；新增 `yak141` family 纠正 `yak_141` 被 `yak1` 螺旋桨家族误吞的问题，并为 `kfir_c7`/`kfir_canard`
    保留不同迎角 exact。`local_test_logs/profile_candidate_diff_batch3.json` 显示 20/20 当前条目匹配候选；报告文件仍不入库。
  - **2026-07-02 温度阈值复核**：移除 `_default` 温度阈值，避免未知载具和未校准喷气机继承一套错误过热线；活塞机温度仍由螺旋桨 class 兜底。
    按 gszabi99 FM `Temperature.Load*` 表给 Su-30 家族/精确条目、F-15E、F/A-18C late、J-10A、J-11B、Rafale C F3、JAS39E
    补入 exact/family 的 `turbine_temp_*` 与 `oil_temp_*` 候选；Su-30 真机样本显示涡轮约 787/788℃、油温约 100/101℃时出现 HUD
    `engine_overheat/oil_overheat`，因此 Su-30 采用 turbine warn 790/critical 830、oil warn 100/critical 110。
  - **2026-07-02 全量 Datamine 固定翼补库**：新增 `tools/update_vehicle_profiles_from_datamine.py`，从本地
    gszabi99/War-Thunder-Datamine checkout 批量补齐 fixed-wing exact profile。当前 `vehicle_profiles.json` 约 1469 个 exact 条目、
    283 条 family、约 1.76MB，其中 1345 条带油温候选、480 条带涡轮温度候选、1425 条带结构过载/G 候选、1200 条带燃油消耗/发动机惯性候选；
    自动补库默认保护 `_tested` 条目的实机校准阈值，但允许补入缺失的只读证据字段；跳过直升机/UAV、给 exact 条目补
    `class`，并把 family 可提供但单机候选缺失的字段回填到 exact，避免 exact 截断 family fallback。注意：
    `CritOverload` / `WingCritOverload` 是结构力证据，不是可直接播报的 G 阈值；当前仅入库为维护证据，尚未启用高 G detector。`vehicle_profiles.json` 已从 `char.vromfs.bin_u/config/wpcost.blkx` 回填小型 vehicle profile economy metadata：`rank`、`economic_rank_arcade`、`economic_rank_realistic`、`economic_rank_simulation`、`country`、`unit_class`、`unit_move_type` 覆盖 1377/1469；`economicRank*` 只按经济元数据保存，不宣称等同 UI BR，也不导入 cost tables、reward multipliers、weapons、modifications。通用参数仍按飞模性能段维护，新增/使用 `transonic_jet`、`early_supersonic_jet`、`subsonic_attacker_jet`、`supersonic_attacker_jet`、`modern_high_alpha_fighter`、`heavy_modern_fighter` 等细分 class，并保留旧 class 兼容。`tools/update_vehicle_profiles_from_datamine.py` 会用 unit `model` / `fmFile` 辅助 class 推断，避免 `nt_*` 等模板 id 污染真实 FM 条目的性能段；`tools/update_vehicle_profile_economy_from_datamine.py` 只更新已有 exact profile，不新增重复 alias。
  - **2026-07-03 ID 口径收口**：补库 exact key 的首选候选改为 War Thunder Wiki `/unit/<gameId>` slug / 页面 `gameId`，并用游戏 `/api/processed.vehicle_type` 实测确认；Datamine id / FM 文件名只作为数值证据和无 Wiki/实测确认时的兜底候选。例：`SB 2M-100A` 的 Wiki URL `/unit/sb_2m_100` 与页面 `gameId=sb_2m_100` 对应数据库 key `sb_2m_100`。
  - **2026-07-03 alias 收口**：新增 `tools/vehicle_profile_id_audit.py`、`vehicle_profile_vetted_ids.json`、`vehicle_profile_identity_aliases.json`。当前 4 组 compact alias 已按 Wiki 页面复核并暂存为兼容 alias：`a_10a_early/a-10a_early`、`a_10c/a-10c`、`f-5th_thailand/f_5th_thailand`、`lagg-3-8/lagg_3_8`；未获得实机 `vehicle_type` 前不删除别名。
  - **残留风险（已知/可接受）**：未录入且型号数字是已录较短家族数字超串的机型（如未录 ki100 时落到 ki10）会就近归类，
    精确补一条对应家族即可纠正——本质是“尽力而为”层，恒优于直接回退默认。

- [x] **阵亡待命态：被击杀后→重生/观战的假警抑制（契约 v1.7）**（2026-06-21，实测两局采集）
  - **采集发现**（rec_20260621_200538 空军重生 / rec_20260621_202413 陆战观战）：玩家死亡后 8111 座舱遥测
    **冻结在死车残骸**上（空军坠机 ias→0/alt 冻结；陆战 speed=0/乘员减员），processor 仍当活载具，
    持续刷 `stall_critical`/`altitude_low`/`crew_loss` 等假警；**观战他人时地图“自身”坐标漂到被观战者**，
    令 situation/proximity 失真。非重生模式下死后很快离局清缓存，这窗口被掩盖；可重生/观战会一直暴露。
  - **修复**（`wt_server`）：新增 `_update_dead_state_locked`。进入＝`combat.my.deaths` 增加；退出＝
    **先见载具静止(_dead_inert_seen)、再恢复运动(空 ias>150/地 speed>5)或乘员满员**——两段式可区分
    “死亡俯冲(高速但已死)”与“重生起飞/行驶”，不会在坠落途中误退出。其间：清空 `processed.alerts/flags`、
    `situation=null`、`proximity.events=[]`、`_poll_map` 不再生成接近告警；快照与 `/health` 带 `dead` 标志。
    **战绩(K/D)照常**——HUD 带全局名字戳，观战期间他人击杀不会误算到本人（实测 30+ 条全未污染）。
  - **离线验证**：空军 #551 死亡 ENTER→#592 新机升空 EXIT（抑制俯冲+残骸+重生选择 41 帧假警）；
    陆战 #833 死亡 ENTER→#950 重生满员 EXIT（**整段 70 秒观战 crew_loss 刷屏被抑制**，157 帧）；
    两局存活期真实告警(804/154 帧)均未被误抑制。
  - 副带结论：陆战 `indicators` **无 `game_time_sec`**（座舱时钟是空军字段），故回放检测的“时间倒退”
    信号在陆战不可用，陆战仅靠 mission 信号——观战非回放、`mission=running`，正确未判回放。

- [x] **全系统离线回归体检 + 嘉奖泄漏 bug 修复**（2026-06-21）
  - 用 6 录制 + 2 抓包跑全系统回归（`_systest.py`，本地工具，不入库）：A.HUD解析覆盖率 B.回放检测 C/D.processor全管线。
  - **结果**：HUD 覆盖率 8/8 会话 **100%**（未覆盖=0）；回放检测**零误判**（仅 235637 判回放，5 局实战全 False）；
    processor 重跑 ~9100 帧**无异常、无负值/NaN**。
  - **修复真实 bug**：`X 完成了最后一击！`（嘉奖 `final_blow`）旧逻辑会漏进 `combat.feed`（action=`other`/parsed=False），
    污染 `by_action`。根因——`KillTracker.feed` 只跳过通知与系统噪声，**未跳过嘉奖**。改为 `parse_award` 命中即跳过
    （嘉奖由 AwardTracker 独立追踪、绝不计入 K/D），比逐条往 `_SYSTEM_NOISE_MARKERS` 加词更稳健。
    验证：rec_230407 feed 100→99、`other` 从 by_action 消失，K/D 2/0 不变。
  - **排除非问题**：overspeed 抓包 `fuel_critical` 占 626/1058 帧——是测试场俯冲坠机后残骸 `fuel=0` 触发比例回退（非时间制，
    `burn`/`rem_s` 全 None）；真实对局（222414/224329）死亡后离开战局即清缓存，fuel **从不读 0**（fuel<=0 帧=0），故为测试场特例、非生产隐患。

- [x] **录像回放检测与整局降级（rec_20260620_235637，回放刚才那局并拖动时间轴）**（2026-06-21）
  - **回放数据本质不同**（实测）：观战镜头在各载具间切换（`vehicle_type` 在 j_15t/None/dummy_plane 间横跳，
    速度/高度/油量不属于单一玩家）；时间轴往回拖会让同一击杀**重复上报**（击落「三九四九」id=1237/1260 但
    `time` 同为 253 → KillTracker 按 id 去重失效，2 杀被算成 3 杀）；`mission_status` 一进场就是终局 `success`。
  - **判定信号（任一命中即锁定本局为回放）**：① `game_time_sec` 同局内倒退 >5s（回放独有，实战恒增，实测倒退 4 次 vs 0 次）；
    ② 进局 8s 后 `mission_status` 始终未出现 `running` 却已是 `success/fail/undefined`（回放一进场即终局，实战进局是 `running`，打完才 `success`）。
  - **实现**：`wt_server` 新增 `_detect_replay_locked`（fast 组内判定、锁定式）；命中后 `get_snapshot` **整局降级**——
    所有接口仅返回 `{replay:true, note, state, in_battle, timestamp, meta}`，停掉告警/战绩/态势/嘉奖等全部派生上报，
    离开战局复位。`/health` 也带 `replay`。契约 v1.6，接口文档加“回放模式”小节。
  - 离线验证：回放录制喂入判定 → 第 23 帧命中（mission 未 running）；实战录制全程未命中。

- [x] **开局 10 秒告警抑制窗口**（2026-06-20）
  - 进入战局后 `_SPAWN_SUPPRESS_SEC=10` 秒内清空 `processed.alerts`/`flags`（**保留派生量/数值**），
    压掉 air RB 空中生成的失速/低高度等瞬态假警。`_poll_fast` 记进局时刻 `_battle_entry_ts`，离局清空。
  - 单测：进局首帧告警被抑制且 `ias_kmh`/`altitude_m` 仍在；15 秒后 `stall_warning` 正常恢复。
  - 顺带确认低油量逻辑：算出油耗率后**按时间**（warn≤300s/crit≤120s），比例仅回退（crit 15% / warn 30%）。
    `fuel_low` 之前刷屏是时间制（<5min）所致，与比例无关；如需收敛改 `fuel_time_warn_sec`（暂未动）。
    2026-07-03 已按维护手册回归为 `Mfuel/Mfuel0` 比例触发；`fuel_remaining_sec` 仅保留为展示/文案参考，不直接触发低油事件。

- [x] **全量数据审计（5 会话）+ 覆盖盲区清零**（2026-06-20）
  - **HUD 覆盖 100%**：补最后一条未识别播报 `完成了最后一击！`→ 嘉奖 `final_blow`(notable，独立句式)；
    新增嘉奖 `air_killer`/`fighter_savior`/`unscathed`。全 5 会话 HUD 未识别项归零。
  - `kind=='event'` 流全程为空（0 种）——所有 HUD 都走 `damage` 流，无遗漏的 event-kind 内容。
  - `mach` 已通过 `vehicle.mach`（来自 `/state` 的 "M"）暴露给前端，无遗漏。
  - **澄清非 bug**：`ammo_empty` 仅地面分支触发，是坦克待发弹打空的持续**状态**（907 帧来自两局陆战），非误报；
    前端应按"状态"而非"重复告警"消费。
  - **proximity 在空战几乎不触发**（两局纯空战 0 条，陆战 17/127 条）：BVR 高速接近，近距阈值很少命中——
    proximity 主要面向陆战/低空，属特性非缺陷。
  - 观察（低危，暂记录）：air RB 开局空中生成会触发一次 `stall_warning` 瞬态假警。

- [x] **完整局+回场补给分析 & 着陆假警修复（rec_20260620_230407，打到结算 success）**（2026-06-20）
  - **回场补给完整捕捉**：着陆段 ias 1265→0、gear_state=down，随后 **fuel 2709→7171kg 跳升**（补给）、再起飞 ias 回升；
    补给**未引发假死/假事件**（deaths=0）。`mission_status` running→**success**（赢），换局边界正常。
  - 战绩：2 杀 0 死，击落 2 名真人（阵风/F-15C），lethal_hit→重创→击落 序列解析准确。
  - **修复着陆假警（重要）**：着陆/补给整段刷了 `stall_critical`(52帧)/`altitude_critical`(70帧) 假警。
    根因——processor 的起落架抑制判据用 `gears > 0.5`，而 **J-15T 的原始 `gears` 恒为 0.5**（171/171 down 帧），
    `0.5 > 0.5` 永假 → 抑制从不生效。改用可靠的 `gear_state == "down"`（指示灯归并），`gears` 仅作回退。
    单测：着陆构型(gear_state=down,gears=0.5)告警被抑制；空中(gear up)照常告警；回退逻辑 OK。
  - 嘉奖新词条（实测）：`空中杀手`(air_killer)、`战斗机救星`(fighter_savior) 标 notable；`毫发无伤`(unscathed) 标 minor。
  - 观察：air RB 开局空中生成（alt 3000/ias 300/throttle 0）会触发一次 `stall_warning` 假警（spawn 瞬态，低危，暂记录）。

- [x] **战斗嘉奖推送（实验性）+ 嵌套括号载具解析修复**（2026-06-20）
  - **嘉奖实测格式**：`<[战队] 玩家名> (载具) 获得嘉奖"双杀！"`，一血为独立句式 `<玩家名> (载具) 先拔头筹！`，
    **带玩家名+战队+载具**。全样本词表：先拔头筹/双杀/三杀/连续无伤歼敌×3/同仇敌忾（共 11 条，确实罕见）。
  - 新增 `wt_events.AwardTracker` + `parse_award` + `_AWARD_RULES`（数据驱动词表，含 tier/notable）；
    `wt_server` 新增 `awards` 快照字段 + `GET /api/awards`（`?notable=1` 仅高光子集）；带 `is_mine` 标记、换局/进局重置。
  - **notable 高光子集**只推罕见重要的（一血/双杀/三杀/连杀/连续无伤歼敌），常见类（同仇敌忾）默认不推；
    可用于识别敌方高威胁玩家（实测 `玛卡巴卡` 一血+双杀+三杀+连续无伤歼敌全中）。
  - **修复嵌套括号载具解析**：`台风 (AESA)`/`◄EF-2000 (AESA)` 旧正则会只截到内层 `AESA`；
    改 `_split_trailing_vehicle` 做括号配平取最外层——**同时惠及击杀流**里这类载具。
  - 嘉奖仍从击杀流剔除（不污染 K/D），独立成流。

- [x] **跨局污染修复——实战复测通过（rec_20260620_224329）**（2026-06-20）
  - `hud_drain` 标记按预期在进局触发，`dropped=152`（丢弃上一局/重启前积压）；damage id 从 889 起（上局到 887），无重叠。
  - **服务端进局首帧 `combat.my.deaths=0` 并全程保持**，直到真实被击落（`igavu 击落了 tl0sr2`）才变 1。污染消失。
  - 残余噪声继续清理：`X 获得嘉奖"双杀!/三杀!/连续无伤歼敌×3!/同仇敌忾"` 等嘉奖播报并入系统噪声过滤；本局 `other` 清零。
  - 复确认 `weapon_firing` 在 1s 录制里 True=0（导弹瞬时扳机采样抓不到），符合既有结论，非回归。

- [x] **修复跨局事件污染（进局排空 hud 积压）**（2026-06-20，实战发现）
  - **现象**：实战局 `rec_20260620_222414` 里出现上一局的 `tl0sr2 (哈立德 I) 已被摧毁`，被实时服务
    错算成本局一次 `you_died`（服务端 `combat.my.deaths` 在进局首帧即为 1，本应 0）。
  - **根因**：8111 `/hudmsg` 是**跨对局保留**的滚动缓冲、id 不随换局归零；`events` 组仅在对局内轮询，
    连续运行时靠游标自然隔离，但**服务(重)启/重连后游标为 0**，进入对局首拉会把上一局残留整批返回并喂进 tracker。
  - **修复**：
    - `wt_telemetry.WarThunderClient.drain_hud()`：推进 `lastEvt/lastDmg` 游标但丢弃事件。
    - `wt_server`：新增 `_hud_drain_pending`（初值 True，覆盖冷启动即在对局；`fast` 线程在进局沿置位）；
      `_poll_events` 首轮先 `drain_hud()` + `tracker/notices.reset()` 再正常增量，录制打 `hud_drain` 标记。
  - **验证**：桩客户端集成测试——排空后 `total_events=0`、`my.deaths=0`；本局真实被击落后 `deaths=1`；
    对照旧逻辑会得 `deaths=2`（含一次假死）。PASS。
  - 注：连续运行的换局（无重启）本就靠游标隔离，无此问题；本修复主要加固(重)启/重连/冷启动场景，并使录制更干净。

- [x] **首次实战验证（rec_20260620_222414，~6 分钟，开局即被击落）**（2026-06-20）
  - **排队昵称生效确认**：大厅 `GET /api/identity?name=tl0sr2`（修改请求需携带 `X-Neko-Warthunder-Action: 1`）在进对局后第一次 events 轮询即生效，
    服务端 `combat.self={name:tl0sr2, source:manual, confidence:1.0}`（大厅未进局时显示 null 属正常）。
  - **`you_died` 三种措辞全部正确触发 `is_my_death`**：
    - 被真人击落（双方形式）：`Simon_Thunder (苏-30SM2) 击落了 tl0sr2 (歼-15T)` → `shot_down`；
    - 地面被摧毁（单方形式）：`tl0sr2 (哈立德 I) 已被摧毁` → `destroyed`。
  - **修正上一版错误结论**：v1.4 曾写"被真人击落不会出现 `X 击落了 你`"——本局直接证伪：
    被真人击落**确实**是双方形式 `敌 击落了 我(载具)`，`is_my_death` 照常触发。文档已更正。
    被击落前常先有 `敌 致命攻击 我` 伤害日志（`lethal_hit` 非致命，不触发死亡）。
  - `weapon_firing` True=0：开局即被击落、未开火，0 正确（无误报）。
  - 新增噪声过滤：`先拔头筹！`（First Blood 播报，无双方结构）。

- [x] **真人多人空战抓包校准（rec_20260620_212525，~39 分钟，hud 751 条）**（2026-06-20）
  - 修复 3 类**空战伤害日志动词**漏解析（原全落入 `other`/`parsed=False`）：
    `致命攻击`→`lethal_hit`、`重创`→`severely_damaged`、`X 的攻击引燃了 Y`→`set_afire`、`击伤`→`damaged`。
    经数据验证与击杀榜（`击落了`94/`已坠毁`292）**基本不重叠**（受害者多为 AI bot），故**标为非致命**
    （`is_kill=False`，不计 K/D），避免与击杀榜重复计杀。
  - 修复**地面/海军单方阵亡** `X 已被摧毁`→`destroyed`（致命）：原只认 `已坠毁`，导致 PGZ09/哈立德
    阵亡漏计；现 tl0sr2 K/D 由 19/2 修正为正确的 **19/4**。
  - 剔除**系统噪声**（本局 260 条）：`X 已掉线。` / `NET_PLAYER_DISCONNECT_FROM_GAME` / 连接/加入
    不再进 `combat.feed`/`by_action`/`total_events`（`other` 325→0，`total_events` 747→487）。
  - **自动识别在真人混战里会认错人**：榜首常是"死得最多"的敌方（本局 `bombardir_317` 12K/21D
    活跃度最高，而真正的自己 tl0sr2 19K/2D 仅排第 3）。已把自动识别置信度下限 0.5→0.6，
    拥挤对局宁可返回 `null`（`source=auto`）交前端 `/api/identity` 手动设——**再次印证手动昵称是主路径**。
  - `you_died` 措辞确认：被真人击落时，游戏对"你自己"的死亡仍以单方 `已坠毁`/`已被摧毁` 呈现，
    **不会**出现 `X 击落了 你`。
  - 待观察（非阻塞）：`weapon_firing` 在 0.5s 录制里 True 仅 2 帧——导弹发射是瞬时扳机，低频采样
    几乎抓不到；机炮连射更可靠。实时 10Hz `/api/indicators` 命中率高于录制采样，前端按瞬时布尔消费即可。
  - 待观察：开局曾出现 `domain_label=海军` 且 `vehicle_type=null` 的瞬态（疑似生成阶段域误判），后续留意。

- [x] **完善 HudMessage 事件的处理与推送**（2026-06-20）
  - 击杀（击落/击毁）、重创敌人、引燃（点燃）：原已由击杀解析（`wt_events.py` 的
    `KillTracker` / `combat.feed`）支持，本次确认覆盖完整。
  - 新增**自机技术通知**解析（`wt_events.py` 的 `parse_notice` / `NoticeTracker`），
    关键词驱动地把以下自机状态警告解析为结构化数据：
    - 油温过高（`oil_overheat`）
    - 左右襟翼非对称展开（`flap_asymmetric`）
    - 发动机过热（`engine_overheat`，含液冷机"水温过高"）
    - 动力系统故障（`powertrain_failure`，含"螺旋桨损坏"）
  - 推送：快照新增 `hud_notices` 字段，新增 `GET /api/notices` 接口（见接口文档 4.12）。
  - **已用真机抓包校准**（10 分钟 Ki-61 空战，captures/ 目录）：17 条 HUD 全部正确分类，
    实测命中"左右襟翼非对称展开""水温过高""动力系统故障：螺旋桨损坏""XX 击毁了 YY""已坠毁"。
  - `clean_text` 增加剥离控制图符区（U+2400–U+243F），修掉载具名前的占位字形（如"␗三式战一型"）。

- [x] **自我识别 + 击杀流涉我标记 + 敌我识别说明**（2026-06-20，基于真机抓包）
  - 问题：玩家昵称不能硬编码；前端问能否做敌我识别。
  - 实测结论：遥测层无可靠"阵亡"信号（`indicators.valid` 全程为真）、hudmsg 不暴露其他玩家队伍。
  - 方案（`wt_events.py` 的 `KillTracker`）：
    - **`combat.active_players`：高活跃度玩家列表**（按 击杀+阵亡 降序，含最常用载具）直接上报前端，
      **不区分敌我**——前端据此展示/让用户点选自己（这是当前主交付）。
    - 手动昵称（`--player-name` 或携带 `X-Neko-Warthunder-Action: 1` 的 `GET /api/identity?name=xxx`）= 权威，置信度 1.0；
    - 无昵称时 `combat.self` 按活跃度给 best-effort 猜测（`source=auto` + `confidence`）；
    - 击杀流每条加 `involves_me/is_my_kill/is_my_death`；
    - 过滤无载具无战队的 AI 地面单位（如 M19A1/史蒂倍克 US6），不计入玩家。
  - 敌我识别：地图层用 `map_objects[].faction`（队伍颜色，可靠）；击杀流层 API 给不了其他玩家队伍，故不做，只标"涉及我"。
  - 修复：中文"已坠毁"解析 bug（victim 误为 `tl0sr2 (三式战一型) 已`，现正确为 `tl0sr2`）。

- [x] **新增遥测字段：开火状态 / 游戏内时间 / 起落架收放 / 对地任务目标点**（2026-06-20）
  - 开火状态：`indicators.weapon_firing`（bool）= 武器扳机 `weapon1/2/3` 任一 >0.5。
  - 游戏内时间：`indicators.game_time`（`"HH:MM:SS"`）+ `game_time_sec`（当日秒数）；
    注意 `clock_hour` 含小数（时+分/60），只取整数小时，分秒用 `clock_min/clock_sec`。
  - 起落架收放：`indicators.gear_state` = `down`/`up`/`moving`/null，由三盏指示灯
    `gear_lamp_down/up/off` 归并（固定起落架机型无此三键→null）。
  - 对地任务目标点：`situation.ground_targets`（轰炸点等），含坐标/网格，有自己时附距离/方位。
  - **顺带修 bug**：`bombing_point` 是红色图标，旧逻辑按颜色误判成 `enemy` 并计入 `enemy_count`/
    `enemies[]`。现 `wt_geo` 与 `Telemetry.enemies/allies` 均按类型过滤（仅 `aircraft`/`ground_model`
    算作战单位），轰炸点改归 `ground_targets`。
  - 其余探查到的字段（油压/进气压/弹药细分等）按用户要求暂不做。

- [x] **补 overspeed 超速告警（前端 DTO 冻结前必须项）**（2026-06-20）
  - `vehicle_profiles.json`：`_default`/各 `_classes` 加 `overspeed_warn_kmh`/`overspeed_critical_kmh`
    （喷气类另加选填 `overspeed_warn_mach`/`overspeed_critical_mach`，IAS 与马赫取或）。
  - `wt_processor.py`：新增 `_process_overspeed`，产出 `overspeed_warn`/`overspeed_critical` 两级 flag；
    **不受 `suppress_when_gear_down` 抑制**（放起落架/襟翼更易撕裂）。
  - 阈值是按类别工程估值（biplane 380/450、prop 720/820、bomber 550/650、early/attacker jet 1000/1100
    +mach 0.86/0.92、modern jet 1350/1500、jet_bomber 850/1000），**尚无真机校准**。
  - 接口文档 4.5 告警代码表已补；契约版本号升 `v1.3`。

- [x] **回应前端 you_died/player_name 诉求 + 契约版本号**（2026-06-20）
  - 实测确认 `state.valid`/`indicators.valid` 整局 1181 帧从不翻转 → 明确告知前端 `you_died` 用
    `combat.feed` 的 `is_my_death` 新 id（唯一可靠源），不要走 valid 跳变。接口文档 4.10 加「事件消费建议」。
  - `player_name` 注入：`--player-name` + 运行时 `/api/identity`，已在 `docs/待办事项.md` 标记完成。
  - `后端接口文档.md` 顶部加契约版本与变更历史（`v1.3`）。

- [x] **overspeed 真机断翼校准（modern_jet / J-15T）**（2026-06-20，5Hz 抓包 `captures/*overspeed*`）
  - 实测 J-15T 俯冲 **~1609 km/h IAS（M1.91, 5903m）断翼**；断翼后高度仍降但 IAS 持续骤减（气动损坏阻力），曲线铁证。
  - 验证告警：`overspeed_warn` 在 IAS 1358（断翼前 ~7.5s/250km/h）、`overspeed_critical` 在 IAS 1513
    （断翼前 ~3.6s/**96km/h**）触发 → `modern_jet` 的 `warn 1350/crit 1500` 窗口合理，已标 `j_15t._tested`。
  - **关键认知**：断翼在遥测里**无直接信号**（hudmsg 无、`/state` 无结构损坏字段），只能靠 IAS 阈值做
    【预测式预警】，无法事后检出撕裂瞬间——印证 overspeed 用阈值告警是唯一可行路径。
  - 抓包时发现：实时 `/api/processed` 的 overspeed 未触发，因 8112 服务跑的是改 overspeed 前的旧代码；
    **改完 processor/profiles 后需重启 8112 才生效**（离线回放新代码已验证正确）。

- [x] **服务端数据转存（调试开关，面向长真实对局数据收集）**（2026-06-20）
  - 新增 `wt_recorder.py`（`SessionRecorder`）+ 接入 `wt_server.py`。默认关闭。
  - 开关：启动 `--record`（+`--record-dir`/`--record-interval`/`--record-segment-mb`），
    或运行时 `GET /api/record?on=1` 开 / `?on=0` 关 / 无参查状态；修改请求需携带 `X-Neko-Warthunder-Action: 1`。
  - 存储格式：**JSONL + 大流(frames)分段滚动 gzip**——当前段明文(可即时 tail/崩溃只丢末行)，
    写满阈值后台压成 `.NNN.jsonl.gz`(实测重复数据压缩比 ~110x，真实遥测约 10~20x)，适合长期/永久留存。
  - 分流避免 O(n²)：`frames`(定频快照，剔除累积数组) + 增量 `hudmsg`/`chat`/`proximity` + `events`(标记) + `meta.json`。
  - 一次会话可跨多局，离开战局写 `battle_reset` 标记供离线切分。`records/` 已加 .gitignore。
  - 为何不用 Parquet/SQLite：前者不利流式追加且需依赖；后者不利即时人读。JSONL+gz 最契合“即时读写+长期留存”。

- [x] **陆战数据收集 + weapon_firing 修复**（2026-06-20，PGZ09 自行高炮 ~9.6 分钟录制）
  - **修复 `weapon_firing`**：原只查 `weapon1/2/3`；实测空军 J-15 用的是 `weapon2`/`weapon4`（无 1/3），
    会漏报。改为**扫描所有 `weaponN` 键**任一>0.5；陆/海战 indicators **无 weaponN 键 → 返回 `None`**
    （不适用，区别于空军“未开火”的 False）。验证：J-15 抓到 5 帧开火、PGZ09 全 None。类型改 `bool|None`。
  - **陆战遥测观察**（数据全在 `/indicators`，`/state` 仅 `valid` 一个键，飞行参数全空）：
    `crew_current/total`(实测掉到 1/3)、`gunner_state`/`driver_state`(0/1 乘员击穿)、`stabilizer`(0/1)、
    `gear`(0~8 档)、`rpm`、`speed`(-15~53，负=倒车)、`gear_position`(-2~6) 均正常；
    `first_stage_ammo` 对该高炮恒 -1/0（无意义，SPAA 无一级待发弹概念）。
  - **陆军接近告警验证 OK**：500m 内 MediumTank/SPAA 正确报，含距离/方位/中文类别。
  - 未发现值得新提取的陆战字段（仅 crew_distance/driving_direction_mode/roll_indicators_is_available 三个边角键，无价值）。
  - ⚠ **数据质量注意**：本局前 6 条 HUD 是上一局 J-15 的残留（8111 hudmsg 缓冲跨服务重启未清，
    新服务以 lastDmg=0 拉到了 backlog）。离线分析按 `battle_reset` 标记切分，并注意战局初的旧消息。
    → **已修复**（见上"修复跨局事件污染"）：进局首轮排空积压，实时统计与新录制不再受此污染。

## 后续可调优（待真机抓包校准）

- [ ] **校准其余类别 overspeed never-exceed**：modern_jet 已校准；二战螺旋桨/双翼/早期喷气仍为保守估值，
  需各抓一次"俯冲到撕裂"微调（螺旋桨撕裂还会进 hudmsg，更易定位；注意零战等早撕裂机型与喷气马赫线）。

- [ ] **再抓气冷机/喷气机样本校准 `oil_overheat`**：本次是液冷机（Ki-61），实测过热报
  "水温过高"（归 `engine_overheat`）；`oil_overheat`（油温/机油）尚无真机样本命中，
  需用气冷活塞机或喷气机再抓一局确认其确切措辞。
- [ ] **按需扩充通知类型**：如自机着火、被照射、弹药殉爆等若在 hudmsg 中出现，
  可在 `_SELF_NOTICES` 增条目。注意避免与击杀流的双方动词（点燃了/set afire）冲突。

> 说明：`KillTracker` 已跳过被 `NoticeTracker` 识别的自机通知，避免像
> “襟翼非对称…可能导致失控”被 `失控`→`wrecked` 规则误判成击杀事件污染战绩。

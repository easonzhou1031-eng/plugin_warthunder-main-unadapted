# Handoff 2026-07-23

## Snapshot

- Source repository: `CN-Zephyr/project-N-E-K-O-Warthunder-8111-data-plugin`.
- Working branch: `agent/isolate-cross-domain-runtime-state`; base commit `34f94cc` contains the redesigned panel and two-step onboarding.
- Host integration branch: `CN-Zephyr:warthuder`; the previous upstream PR `Project-N-E-K-O/N.E.K.O#2371` is merged, so current synchronization is branch maintenance rather than an open-PR update.
- Current automated baseline: `uv run pytest -c tests/pytest.ini tests -q` -> `575 passed`.
- `tests/run_logic_tests.py` now expands simple `pytest.mark.parametrize` cases, so the documented one-command logic check and pytest count agree at `575/575`.
- The standalone repository is the current release source. The embedded `N.E.K.O/plugin/plugins/neko_warthunder` copy is aligned for the current runtime maintenance, and the host boundary gate now also compares process management, identity actions, the 8112 server and recorder lifecycle files.
- Existing profiles are migrated once from the legacy urgent direct-output default to the current host-polished speech path. The migration persists `plugin_owned_urgent_output_enabled=false`, retries after write failure, and preserves later explicit user opt-in.
- Broadcast preferences are implemented and persisted: quiet/standard/active pacing affects only noncritical cooldown/backpressure, while five category switches control general safety, combat results, fixed radio, awareness, and lifecycle output. A settings action restores standard pacing and all five ordinary categories without changing nickname, dialogue policy, `dry_run`, or output start/stop state. Critical safety and death reminders cannot be disabled by these switches.
- Offline RC status: `tools/preflight.py --run` and `tools/release_readiness.py --run` pass; the release verdict is `ready_for_final_live_smoke` with `go_no_go=go_dry_run_final_smoke`.
- Last offline package after the 2026-07-16 maintenance: `dist/neko_warthunder-0.1.0-20260716-offline-rc.neko-plugin`; size `265500` bytes, archive SHA256 `521C396BFD7C5421C21C70435B89F3DBF501793FDA8EAC1061F928E54DB0AC0E`, and verified payload hash `11a45143d984c43a48b09f878dda6e34fd33be2afab43c7e7a9f6001f7fa9378`. It predates the 2026-07-23 maintenance and is not the next release artifact.
- Package inspect, `tools/package_artifact_gate.py`, required/excluded file audit and temporary-directory installation smoke pass; `.ruff_cache` is explicitly excluded from future builds.
- `tools/build_release_candidate.py` now turns the official release check, artifact gate, official payload verification and isolated installation smoke into one atomic command; the safe-activity RC passed this complete pipeline.
- Release posture: **feature-complete and offline-stabilized for the current RC scope; focused live evidence was intentionally deferred, not passed**.

## Completed Since The Previous Handoff

- Runtime state is isolated across air, ground, naval and non-battle interruptions; mode changes re-arm the relevant awareness detectors without leaking stale state across domains.
- Hosted UI was redesigned around three operator views: an overview for normal use, a filterable activity history, and diagnostics for explaining the runtime chain.
- The activity history is backed by an always-available, in-memory 20-item safe ring. It stores only event/stage/outcome metadata; debug timeline opt-in and raw-text privacy boundaries are unchanged.
- Diagnostics can copy a versioned, allowlisted support summary containing connection, battle-domain, safety, preference and recent decision/output codes. It excludes identity, chat/HUD, targets, vehicles, URL/PID, error text and prompt/payload text.
- First-run onboarding is two steps: set the War Thunder nickname, then learn the small set of persistent controls. It can be skipped and reopened from settings.
- Broadcast start/stop, emergency pause/resume, sound test, refresh, nickname editing and dialogue policy remain reachable without exposing raw telemetry or free text.
- Settings include a one-click recommended broadcast reset; it does not add controls or activity items to the overview screen.
- Panel safety/output status checks are centralized, dead styles were removed, controlled selects no longer carry redundant `selected`, and the duplicate dialogue-policy control was removed from settings. The overview footer remains the single policy control.
- The standalone source now includes the host-integrated context-state, repeat-collapse, preemption and takeoff-protection fixes. The host boundary gate follows the current modular `main_logic` layout and ignores line-ending-only differences when checking the embedded plugin copy.
- Eight plugin manifest locales are present with matching keys. The large Hosted UI body remains Chinese-first for this RC.
- All existing privacy, dry-run, replay, free-text, mode/domain, output freshness and V2 real-output guards remain in place.
- Managed 8112 startup now binds only to the configured loopback host, respawn resets discard the pre-reset processed frame, free-text activity metadata no longer carries source-provided codes, and the standalone tests follow these embedded-runtime contracts.
- 2026-07-16 maintenance closes the browser-facing 8112 boundary: wildcard CORS is removed, loopback `Host` is validated, and identity/record mutations require `X-Neko-Warthunder-Action: 1`; explicitly trusted browser origins can be added with repeatable `--cors-origin` arguments.
- Data-layer health now refreshes from every telemetry poll, managed-process exits are reflected in diagnostics, startup failures close log handles, embedded shutdown closes the HTTP socket, and changing the configured 8112 URL/auto-start setting restarts only the plugin-owned service and restores saved identity after a healthy restart.
- 2026-07-23 maintenance adds stable `battle_id` / `life_index` semantics, confirmed-respawn counters, transient 8111 probe grace, per-match plugin state reset, and corrected horizontal clock reporting. Same-match respawn no longer looks like a new battle.
- Promotion-only relaxed defaults and immediate terminal-output special cases are excluded from formal source. `dry_run=true` and `v2_live_verified_real_output_enabled=false` remain the release defaults, and battle-end output follows the normal arbitration/backpressure path.
- Prompt contracts no longer prescribe a fixed emotional response for spawn, kill, death, or battle end; they constrain only verified facts, domain vocabulary, response length, and non-takeover safety.

## Current Product Boundary

- Do not add new event families before the RC evidence pass unless a verified defect blocks testing.
- Keep `dry_run=true` by default.
- Keep `v2_live_verified_real_output_enabled=false` until fresh rear/six, sustained tailing and objective evidence is recorded.
- Keep hudmsg/awards/free-text real speech closed; only safe dry-run observability is approved.
- Crew, role and first-stage ammo remain data/panel facts and do not produce speech. Ground status speech keeps only a genuine `ground_laser_warning`.
- The plugin/runtime data boundary remains HTTP `:8112`; do not import the vendored data layer into the plugin path.
- Do not add War-Thunder-specific behavior to host core. Continue using the generic callback metadata reservation.

## Next Stage

### R1 - RC stabilization (completed 2026-07-15)

1. `uv run python tests/run_logic_tests.py` and full pytest both pass with `524` cases.
2. `uv run python tools/preflight.py --run` and `uv run python tools/release_readiness.py --run` pass.
3. The standalone `ui/panel.tsx` and host-integrated copy are identical; the host contract gate also passes.
4. The offline RC has been rebuilt and verified. Future rebuilds must use `uv run python tools\build_release_candidate.py`; do not manually publish a package that skipped the artifact or temporary-install gates.

### R1.1 - Offline activity center (completed 2026-07-15)

1. Keep the overview's compact recent list and add a dedicated activity page with all/submitted/recorded/suppressed filters.
2. Expose only a bounded safe summary ring to the panel; do not enable the debug timeline or store raw player/chat/HUD/prompt text.
3. Keep all event detection, speech policy, `dry_run`, and real-output gates unchanged.

### R1.2 - Safe diagnostic summary (completed 2026-07-15)

1. Add a copy action to the diagnostics page without changing the overview layout.
2. Build the support text from a fixed safe-field allowlist; do not copy identity, raw telemetry text, targets, transport details or exceptions.
3. Reuse the host clipboard helper so browser development and Electron distribution share the same fallback behavior.

### R2 - Focused evidence and release resynchronization

1. Verify a teammate sending the same fixed-radio command does not emit `player_radio_command`.
2. Capture fresh `replay=true` suppression and awards/free-text dry-run summaries without raw text.
3. Preserve the July local rear/six, sustained-tailing and close-objective recordings as evidence inputs, but rerun the formal sample/evidence gates before opening any V2 real output.
4. Recheck takeoff/respawn protection, stale-event expiry, critical replacement, short single-line speech metadata and target-session routing.
5. If a suitable ground vehicle is available, record a genuine `LWS=1` sample; do not synthesize or infer it from standby states. Mode labels and lineup size remain unavailable from the current 8111 contract and must not be guessed.

### R3 - Final RC smoke

1. Install the new package into the packaged host and confirm only one plugin copy is discovered.
2. Start with dry-run, save safe `live_monitor` JSON/JSONL and numeric transcript metrics, then pass `final_smoke_evidence_gate.py`.
3. Run only the already-approved small real-output smoke after dry-run evidence passes.
4. Keep unverified V2 and free-text real output closed even if the rest of the RC is accepted.

## Do Not Claim Yet

- Do not call the plugin a final public release.
- Do not claim teammate radio isolation is live-verified until the negative case is recorded.
- Do not claim the affected user's arcade/menu misclassification is fixed without an affected-environment frame.
- Do not claim V2 live evidence complete until objective and fresh runtime-output evidence pass the gate.
- Do not claim real laser warning behavior is verified without an actual `LWS=1` sample.

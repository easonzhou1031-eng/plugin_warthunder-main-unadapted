# 待办事项

> 本文件给**数据层开发者**看：列出 Battle Awareness（猫娘副驾驶）侧在 **DTO 冻结前**需要数据层补齐的事项。
> 两侧唯一边界 = HTTP `:8112`（`/api/telemetry`）。以下都是"在你现有架构上加字段/补解析"，不改你的结构。
>
> **数据层回复（2026-06-21，契约 v1.6）**：优先项已全部完成，且已合并到当前独立插件仓库。后续重点不是“等数据层补字段”，而是插件侧适配/验证 v1.6 DTO。

> **2026-07-10 修复更新**：三个 P0 数据合同问题已在数据层修复，完整 pytest 为 `457 passed`。当前从“等待代码修复”转为“真机 dry-run 复验”，详见 `handoff-20260710.md`。
>
> **2026-07-27 纠错更新**：用 `local_samples` 归档里的完整 8112 快照流回放，证实并修正了多处判定——交火压力误判（旧规则 69.1%/51.3% 的时间误判）、仲裁缓冲约五分之一事件在推送前过期、`low_fuel` 每局重报三次、危急心跳阈值取 8s 导致机制永不触发；同时下调了「重生重播整局击杀」的严重度（真实录制不复现，详见交接文档）。当前 pytest 与一键逻辑测试均为 `579/579 passed`，详见 `handoff-20260727.md`。

> **2026-07-24 维护更新**：新版概览/活动/诊断面板、两步首次使用教程、跨模式运行态隔离和播报偏好/频率设置已完成；当前 pytest 为 `579 passed`，一键逻辑测试为 `579/579 passed`。原始聊天禁止持久化；继续按保守发布默认和真实链路验证收口，详见 `handoff-20260715.md`。

- [x] **T-Broadcast-Preferences：播报偏好与频率设置**
  - 设置页提供安静、标准、活跃三档，按比例调整非危急事件 cooldown、全局限流和输出背压。
  - 提供一般安全、战果、固定无线电、态势感知、开场收尾五类开关并持久化到插件运行状态。
  - 提供“恢复推荐播报设置”，一键恢复标准频率并重新开启五类普通播报；不修改昵称、插话规则、`dry_run` 或播报启停状态。
  - 安全边界：critical 安全事件和 `you_died` 始终保留，偏好开关不改变抢占优先级，也不接管宿主音量。

- [x] **T-Safe-Activity：安全活动中心**
  - 运行时保留最近 20 条安全活动摘要，仅包含事件、阶段、结果、原因和时间等元数据；不保存玩家昵称、聊天、HUD、prompt 或 payload 原文。
  - 面板新增“活动”页，提供全部、已提交、仅记录、未输出筛选；主页最近活动仍保留为快速摘要。
  - debug timeline 仍默认关闭，活动中心不改变事件检测、播报策略或真实输出门禁。
- [x] **T-Safe-Diagnostics：安全诊断摘要**
  - 诊断页支持一键复制排障摘要，使用固定字段白名单记录连接、战局域、安全门、播报偏好和最近决策/输出代码。
  - 摘要不包含昵称、聊天、HUD、目标、载具、数据层 URL/PID、错误原文、prompt 或 payload 原文；不改变概览页和运行逻辑。

## 优先（DTO 冻结前必须）

- [x] **P0：收缩不可信的陆战岗位/LWS 告警**
  - 真机证据：满员 `4/4` 时 `gunner_state=0`、`driver_state=0`；受损/补员期间还出现 `3/0`、`1/1`，不是稳定布尔语义。
  - 处理：`0` 正常、`1` 无人补位、`2` 正在补位；`3` 只透传。LWS 仅在 `lws == 1` 时生成告警。
  - 离线验收：正常/失能/补位/未知岗位值与 LWS `-1/0/1/2/3` 均已覆盖。
  - 待真机：状态 `3` 语义、真实激光照射和 LWS 损坏样本。

- [x] **P0：一级弹药有效基线与同局重生保护**
  - 处理：先观察到正数基线后才允许 low/empty；`dead -> alive` 时 reset processor 并重开 10 秒出生保护；负值/缺失值清空基线。
  - 离线验收：开局 0、不可信 -1、正数降 0 和同车重生首帧均已覆盖。
  - 待真机：开局、耗尽、死亡、同车重生完整序列。

- [ ] **P1：固定无线电队友隔离真机反例**
  - 已通过：两条自己发送的固定无线电，chat id 递增、手动 identity 匹配；`进攻 D 点` -> `player_radio_command / attack_point / D`。
  - 待通过：队友发送同一固定口令时不得产生 `player_radio_command`；仍不记录 raw sender/msg。

- [x] **T-Safety: output text sanitizer** ✅ 已完成
  - 目标：防止猫娘复读不良玩家 ID、`hudmsg`、`combat.feed` 原文，避免辱骂、涉政、擦边、仇恨、广告、联系方式、奇怪符号或 prompt injection 文本进入猫娘输出。
  - 放置位置：`neko_warthunder` 的 `NekoDispatcher` / prompt builder 前。数据层继续保留 raw fact，不负责输出安全；Detector / Scenario / Arbiter 不承担文本过滤职责。
  - 关键策略：
    - raw 玩家名 / hudmsg / combat.feed 原文只进 audit/debug。
    - safe 字段才允许进入 prompt。
    - 默认 generic 文案，不朗读陌生玩家名（例如"一名敌人"、"对手"、"某位玩家"）。
    - `hudmsg` / `combat.feed` 原文默认不进 prompt。
    - 不确定时宁可不读原文。
  - 当前状态：已新增 `adapters/text_safety.py`，并在 `adapters/neko_dispatcher.py` 的 prompt builder 前接入。
  - 验收：sanitizer 单测、dispatcher prompt 测试、`push_message.parts[].text` 不包含 unsafe raw 的合同测试已覆盖；hudmsg / combat.feed / awards 常见自由文本字段族已默认 blocked；最后一次完整 pytest 基线为 `579 passed`。
  - 补强：`tools/release_defaults_gate.py` 已作为发布默认值门禁，确认默认 `dry_run=true`、debug timeline 关闭、V2 未验真实输出关闭，且真实输出背压 / 过期丢弃 / 多杀合并等保守默认仍开启。
  - 补强：`tools/free_text_gate.py` 已作为 hudmsg / combat.feed / awards 去桩前离线发布门禁，并纳入 `tools/preflight.py`；门禁使用合成恶意 payload 验证 prompt、`push_message.parts[].text` 和 safe payload 均不含 raw 文本。
  - 补强：`tools/replay_gate.py` 已作为 replay 降级离线发布门禁，并纳入 `tools/preflight.py`；门禁使用合成 `replay=true` 帧验证 critical flags、owned combat.feed、HUD notices、awards 均不会产生 candidate、prompt 或真实 `push_message`。
  - 补强：`tools/deferred_hud_gate.py` 已作为 deferred HUD notice 离线发布门禁，并纳入 `tools/preflight.py` / `tools/release_readiness.py`；门禁验证 `powertrain_failure` 只可观测、不播报、不泄露 raw HUD 文本。
  - 补强：mode/domain 边界已从 Detector 延伸到提示词输出层；事件 payload、Dispatcher prompt、`push_message.metadata.domain_prompt_contract` 都保留 `domain` 合同，避免模型在陆战/空战/直升机/海战之间串话术。
  - 补强：运行态已记录 `detector_suppressed/free_text_blocked`，首次看到 `awards` / `combat.feed` / `hud_notices` / `hudmsg` / `hud_events` 时只输出安全来源与计数摘要；同时新增 `free_text_activity` dry-run-only 候选，让真机 dry_run 能看到 Detector / Arbiter / Dispatcher 链路，但 `dry_run=false` 仍以 `free_text_dry_run_only` 阻断真实输出。

- [x] **T-Release-Readiness：v1 RC 离线汇总入口**
  - 入口：`uv run python tools\release_readiness.py --run`；JSON / 文本输出中的 `release_scope` 会列出 `ship_status`、`real_output_blockers`、`sample_unproven_items` 和 `next_actions`。
  - 样本：默认 `--run` 不自动跑 ignored `local_samples/` 大样本；需要把本地样本报告也纳入时显式加 `--include-local-sample`。
  - 范围：logic tests、pytest、free-text release gate、replay degrade gate、ownership replay gate、deferred HUD notice gate、proximity/objective awareness gate、V2 readiness / matrix / output policy / completion gate、RC handoff report、synthetic replay、宿主存在时的 host boundary gate / plugin check 与本地样本报告。
  - 边界：不启动前后端，不依赖 War Thunder，不替代最后一轮真机 smoke。
  - 后续关系：kill / death generic 输出已通过真机 dry_run 与 `dry_run=false` push smoke；hudmsg / awards / 其他 free-text 正式播报仍需真机 dry_run 安全验证。数值安全事件不被 T-Safety 阻塞。

- [x] **V2 Proximity / Objective Awareness：接近威胁与任务目标感知**
  - 目标：消费数据层 `proximity.events` / `situation`，为“附近敌人 / 空中威胁 / 后方威胁 / 任务目标点”提供低成本战场态势提醒。
  - 已完成：`BattleState.proximity` / `proximity_events` / `situation`，`ProximityDetector` 按 id 去重，生成 `enemy_nearby`、`air_threat_nearby`、`enemy_on_six`，并在短窗连续近距离后方事件时保守升级为 `tailing_risk`；`GroundTargetDetector` 从 `situation.ground_targets` 生成低优先级 `ground_target_nearby`。
  - Arbiter 策略：普通 `enemy_nearby` 在 `COMBAT_STRESS` 下被低优先级门控压住；`air_threat_nearby`、`enemy_on_six` 与 `tailing_risk` 可在 `IN_FLIGHT` / `COMBAT_STRESS` 下进入队列；critical 安全事件仍优先，`SPAWNING` / `DEAD` 等场景仍抑制 proximity。
  - 输出策略：Dispatcher 只使用距离、方位、钟点、目标类别、网格等 safe metadata，不复读 raw proximity 文本、目标 label 或玩家名。
  - 可观测性：Hosted UI context / 面板暴露 `awareness` 安全摘要；`tools/sample_replay.py` 统计 proximity/situation/ground-target 覆盖率。
  - 门禁：`tools/proximity_gate.py` 已纳入 `tools/preflight.py` / `tools/release_readiness.py`；最后一次完整 pytest 为 `579 passed`，真机证据仍按能力项分别记录，不以宣传素材代替。
  - mode 约束：proximity / situation / objective 事件如果来自空战或直升机域，也会携带 `domain` 到 Dispatcher；prompt 与 metadata 应显示对应 `当前模式`，不要让任务目标或态势提醒借用错误载具角色。
  - 样本状态：2026-06-20 本地样本合并 side-stream proximity 与连续 `situation.enemies` 后，已观察到 `proximity_events=5317`、`proximity_air_events=5300`、`proximity_rear_events=49`、`situation_rear_air_threat_live_items=1906`，并触发 `enemy_on_six=149` / `tailing_risk=44`；仍没有 3000m 内任务目标点候选，下一轮真机统一补测 `ground_target_nearby`。

- [x] **T-V2-Readiness：V2 收口汇总**
  - 入口：`uv run python tools\v2_readiness.py --no-sample`；若要按能力矩阵查看 code/offline/live evidence/real-output policy，运行 `uv run python tools\v2_release_matrix.py --no-sample`；若要合并本地样本证据，运行 `uv run python tools\v2_readiness.py local_samples\data_process_20260620 tl0sr2` 和 `uv run python tools\v2_release_matrix.py local_samples\data_process_20260620 tl0sr2`。
  - 目标：把 V2 代码/离线门禁完成状态和真实样本证据缺口拆开，不再靠人工翻 `sample_replay` / `proximity_gate` 多份输出。
  - 输出：`offline_scope`、`live_evidence`、`release_scope.v2_code_complete`、`release_scope.v2_offline_gate_complete`、`release_scope.v2_live_evidence_complete`。
  - 边界：不声明真机-only 项已完成；后方 / 六点钟、持续尾随、3000m 内任务目标点仍需真实样本或下轮真机验证。

- [x] **T-V2-Output-Policy：V2 真实输出策略门禁**
  - 入口：`uv run python tools\v2_output_policy_gate.py`；机器可读输出使用 `--json`。
  - 目标：把“V2 代码已完成”和“真机证据未齐不能真实播报”拆开，避免后方威胁、持续尾随、任务目标点提醒在缺少现场样本时误进入真实 `push_message`。
  - 策略：`enemy_on_six`、`tailing_risk`、`ground_target_nearby` 默认受 `v2_live_verified_real_output_enabled=false` 保护；dry_run 仍可观察决策链路，真实输出默认返回 `v2_live_evidence_pending` suppress；显式开启 `v2_live_verified_real_output_enabled=true` 后才允许真实推送。
  - 边界：不影响 `enemy_nearby`、`air_threat_nearby` 的既有离线输出合同；不修改 data_layer，不改变 Detector / Scenario / Arbiter 语义，不复读 raw proximity/objective 文本。

- [x] **T-V2-Completion-Gate：V2 完成度门禁**
  - 入口：`uv run python tools\v2_completion_gate.py --no-sample`；合并本地样本证据时运行 `uv run python tools\v2_completion_gate.py local_samples\data_process_20260620 tl0sr2`。
  - 目标：把 V2 readiness、能力矩阵、真实输出策略合并成一个单一 pass/fail 结论，方便维护者判断“代码/离线完成”和“真机证据未齐”的边界。
  - 输出：`verdict=v2_code_offline_complete_live_evidence_pending` 表示 V2 code/offline scope 已收口，但后方威胁、持续尾随、3000m 内任务目标点等 live-only 证据仍需统一真机补样本。
  - 边界：不宣称缺失真机证据已完成；不改变 V2 事件语义，不修改 data_layer，不开放未验证真实输出。

- [x] **T-Final-Smoke-Packet：最终真机 smoke 交接包**
  - 入口：`uv run python tools\final_smoke_packet.py`；机器可读输出使用 `--json`。跑真机前可用 `uv run python tools\final_smoke_evidence_gate.py --safe-transcript-template --output local_test_logs\safe_transcript_metrics.json` 生成猫猫实际回复 metrics 模板；跑完真机后可先保存 `uv run python tools\live_monitor.py --count 3 --interval 1 --json --output local_test_logs\live_monitor_final.json`，再用 `uv run python tools\final_smoke_evidence_gate.py --from-live-monitor local_test_logs\live_monitor_final.json --output local_test_logs\final_smoke_evidence.json` 扫描整段 JSONL 并预填 evidence 草稿；填好无原文 metrics 后优先用 `uv run python tools\final_smoke_evidence_gate.py local_test_logs\final_smoke_evidence.json --safe-transcript local_test_logs\safe_transcript_metrics.json --confirm-mode-domain-boundary` 合并猫猫行数、字数、是否续写、聊天静默、critical 替换观察和 mode/domain 边界确认；没有 metrics 时再用 `uv run python tools\final_smoke_evidence_gate.py local_test_logs\final_smoke_evidence.json --update --confirm-critical-replaced-stale-warning --confirm-user-chat-quiet-window --confirm-short-tts-single-line --confirm-mode-domain-boundary` 合并确认；证据验收入口：`uv run python tools\final_smoke_evidence_gate.py local_test_logs\final_smoke_evidence.json`；统一复验入口可追加 `--final-smoke-evidence <path>`。
  - 推荐合成入口：交接包中的 `evidence_from_monitor_and_transcript`，即 `uv run python tools\final_smoke_evidence_gate.py --from-live-monitor local_test_logs\live_monitor_final.json --safe-transcript local_test_logs\safe_transcript_metrics.json --confirm-mode-domain-boundary --output local_test_logs\final_smoke_evidence.json`，可把 monitor metadata、猫猫回复 metrics 和 mode/domain 边界确认一步写成最终 evidence。
  - 推荐 metrics 入口：交接包中的 `safe_transcript_record`，即 `uv run python tools\final_smoke_evidence_gate.py --record-safe-transcript --reply-chars <count> --reply-lines 1 --confirm-critical-replaced-stale-warning --confirm-user-chat-quiet-window --output local_test_logs\safe_transcript_metrics.json`，只保存数字和确认 flag。
  - 推荐演练入口：交接包中的 `evidence_rehearsal`，即 `uv run python tools\final_smoke_evidence_gate.py --rehearsal-output-dir local_test_logs\final_smoke_rehearsal`；输出带 `rehearsal_only=true`，只证明证据流程，不替代真机证据。
  - 目标：把 v1/v2 handoff、go/no-go、必跑命令、V2 live evidence 缺口、runtime focus checks、remaining live actions 和安全边界集中到一份最终真机前交接包。
  - 输出：默认 `go_no_go=review_required_run_offline_gate`；本轮 `release_readiness.py --run` 通过后可用 `--offline-gates-passed` 得到 `go_no_go=go_dry_run_final_smoke`。同时输出 `handoff_status`、`v2_live_evidence_complete`、V2 逐能力矩阵、observed/triggered 计数、`runtime_focus_checks`、`remaining_live_actions`、`safety_boundary.dry_run_first`、`safety_boundary.raw_text_printed=false`；evidence gate 要求四个 P1 runtime focus check 全部 pass，且不存 raw 文本。
  - 边界：只做只读汇总；不启动前后端、不接触真机、不宣称真机-only 项完成。

- [x] **T-RC-Handoff-Report：维护者 RC 交接报告**
  - 入口：`uv run python tools\rc_handoff_report.py --no-sample`；已跑过 `release_readiness.py --run` 后可用 `uv run python tools\rc_handoff_report.py --offline-gates-passed`。
  - 目标：给维护者/合作者一份大白话交接：V1 离线门禁状态、V2 code/offline 完成度、live evidence pending、安全边界、final smoke go/no-go 和下一步动作。
  - 边界：只读汇总，不启动宿主、不启动 War Thunder、不依赖本机样本；不记录 raw 玩家名、HUD、combat.feed、awards、proximity 或 objective 文本。

- [x] **T-Package-Artifact-Gate：分发包内容门禁**
  - 入口：构建后运行 `uv run python tools\package_artifact_gate.py <path-to-neko-plugin>`；机器可读输出使用 `--json`。
  - 目标：检查插件包身份、运行必需文件、8 个 locale、压缩包路径安全和开发文件排除，避免缓存、测试、文档或本地运行态混入交付包。
  - 结果：首轮门禁发现 `.ruff_cache` 被打进 offline RC；`pyproject.toml` 已增加显式排除并重建，当前产物通过门禁和临时安装 smoke。
  - 边界：只验证构建产物完整性，不替代源码 preflight、宿主兼容检查或真机证据。

- [x] **T-RC-Builder：统一离线 RC 构建入口**
  - 入口：`uv run python tools\build_release_candidate.py`；已有同名产物时显式加 `--force`，已单独跑完 pytest 时可显式加 `--skip-tests`。
  - 流程：宿主官方 release check → `package_artifact_gate.py` → 官方 payload verify → 隔离临时安装 smoke → 原子发布到 `dist`。
  - 安全：临时安装只使用系统临时目录，不触碰用户真实插件/配置；任一门禁失败都不会发布半成品或覆盖已有包。
  - 验收：纯离线合同测试和一次临时端到端构建均通过，临时包 payload hash 与当前 offline RC 一致。

- [x] **补 overspeed（超速）告警 flag** ✅ 已完成（v1.6 合并）
  - 做法：`vehicle_profiles.json` 加 `overspeed_warn_kmh` / `overspeed_critical_kmh`（逐类 never-exceed，
    喷气/喷气轰另给选填 `overspeed_warn_mach` / `overspeed_critical_mach`，IAS 与马赫判据取或）；
    `wt_processor.py` 加 `_process_overspeed`，产出 `overspeed_warn` / `overspeed_critical` 两级 flag。
  - 注意：① **不受起落架抑制**（放襟翼/起落架时更易撕裂，反而更危险）；② 阈值是按类别的**工程估值**，
    游戏不存静态限速、各机撕裂线差异大，**尚无真机校准**，需后续按机型微调（同 stall/aoa 性质）。
  - 验收：`/api/processed.flags` 已出现 `overspeed_warn` / `overspeed_critical`，见接口文档 4.5 告警代码表。

- [x] **完成 hudmsg 事件解析与推送** ✅ 已完成（v1.2，真机抓包校准）
  - `combat.feed`（`KillTracker`）：击落/击毁/重创/引燃，带**递增 id**、`is_kill`、施/受动者载具与战队、AI 标记。
  - `hud_notices`（`NoticeTracker`）：自机技术通知（水温过高/油温/动力故障/非对称襟翼），见接口文档 4.12 + `/api/notices`。
  - 已用 10 分钟 Ki-61 空战抓包验证 17 条 HUD 全部正确分类。

- [x] **击杀归属判断（is_kill + 玩家本人）** ✅ 已完成（v1.2）
  - `feed[]` 每条带 `involves_me` / `is_my_kill` / `is_my_death` + `killer_is_ai` / `victim_is_ai`，可区分
    "我击杀 / 我被击落 / 队友阵亡 / AI"。**消费建议见接口文档 4.10「事件消费建议」**。
  - ⚠ **死亡信号关键结论**：实测整局 `state.valid` / `indicators.valid` **全程为 true，从不翻转**，
    你们设计的 `you_died` 走 valid 跳变这条**不可用**；请改用 `feed[]` 中 `is_my_death==true` 的新 id（唯一可靠源，
    覆盖被击落与自身坠机）。`mission_status` 只用于 `battle_end`。

- [x] **player_name 注入方式确认** ✅ 已完成（v1.2）
  - 注入方式：① 启动参数 `--player-name`；② **运行时 `GET /api/identity?name=昵称`（权威，置信度 1.0）/ `?clear=1` 清除**；修改请求需携带 `X-Neko-Warthunder-Action: 1`。
  - 未注入时 `combat.self` 按活跃度给 best-effort 自动猜测（`source=auto`，置信度不足时 `name=null`，≥0.5 才用于统计）。
  - 插件面板：只支持手动填写/保存/清除玩家名，并把手动玩家名持久化到插件配置；`combat.active_players` 只作为安全摘要/调试数据，不提供候选点选。详见接口文档 4.10。

## 配合事项（非阻塞，但有助于 G2~G5 真机校验）

- [~] 提供若干**真实 `/api/telemetry` 抓包样本**（按场景）。
  - 现状：已有 1 局完整 Ki-61 空战原始抓包（`wt_capture.py` 产物，含 state/indicators/hudmsg/map 等）。
  - 2026-06-23 真机 smoke 已覆盖失速 warning/critical、低空 warning/critical、超速 warning/critical、低油 warning/critical、过热 warning/critical、手动 identity、owned combat.feed 归属字段、`you_killed` / `you_died` dry_run 与 `dry_run=false` push；2026-06-20 本地样本已覆盖空中 proximity / situation / ground target items，并已通过 `situation.enemies` + side-stream proximity 覆盖后方/六点钟与 `tailing_risk` 离线触发；仍缺 3000m 内任务目标点候选、replay=true、awards/free-text、油温/发动机细项等样本。
- [x] 接口字段/枚举变更标注**契约版本号** ✅ 已加：`后端接口文档.md` 顶部「契约版本」+变更历史，当前 `v1.6`。

## 插件侧新增 TODO（数据层 v1.6 合并后）

- [~] **真机 dry_run smoke**（2026-06-21 / 2026-06-23）
  - 已通过：Hosted UI context/action、`pause` / `resume`、`test_say`、`spawn`、`overspeed_warn` / `overspeed_critical`、`stall_risk`、`low_alt_danger`、`overheat`、`you_killed` / `you_died` 的 dry_run 输出，以及手动 identity / owned kill-death 字段接缝。
  - 已补排障能力：T-Observe 普通模式暴露 `last_event` / `last_decision` / `last_output_status`；debug timeline 使用内存 ring buffer 且默认关闭。
  - `dry_run=false` 已验证：`test_say`、`you_killed`、`you_died` 均进入真实 push，`last_output_status=dispatcher_pushed / push_message_accepted`，并由现场确认猫娘实际开口。
  - 未见：`PLUGIN_UI_ACTION_FAILED`、Traceback、TTS/push 报错。
  - 已补插件侧映射：`hud_notices.feed[].code=engine_overheat/oil_overheat` 可生成现有 `overheat` 事件，raw HUD 文本不进 prompt。
  - 已补 L9 起飞调参：高度判断优先 `radio_altitude_m`，但固定翼不假定必有 AGL；`takeoff_low_alt_grace_seconds` 默认 45s；`takeoff_radio_altitude_enter_m=10` / `takeoff_radio_altitude_exit_m=40` 用于 AGL 可用时的贴地滑跑迟滞保护。保护期内压制 `low_alt_danger`；`overspeed` 只在 AGL 贴地保护内，或 AGL 缺失但仍处于起飞保护期且起落架放下/运动中时压制，不影响 `stall_risk` / `overheat` / `low_fuel` / `you_died`。
  - 已补 Hosted UI 信息架构整理和中文化：连接状态、战场状态、安全控制、最近决策、最近输出分区清晰；主要状态标签、场景、风险等级、数据层模式和身份识别来源显示中文。
  - 仍缺：replay=true 真实样本验证、awards/free-text 真机 dry_run 安全验证；油温/发动机细项等数据层数据库补齐后再测。

- [x] **L8：数据层子进程最小编排**
  - 已完成：`data_layer_auto_start` 默认开启；若 `:8112` 已健康则标记为 `external` 且不接管；若缺失则启动 vendored `data_layer/data process/wt_server.py --port 8112` 并标记为 `managed`。
  - 关闭策略：只关闭插件自己拉起的 managed 进程；手动或外部启动的 `:8112` 不误杀。
  - 可观测性：Hosted UI/status 暴露 `data_layer.mode`、`pid`、`started_by_plugin`、`health`、`last_error`。
  - 2026-06-26 本地自验证通过：`8112` 关闭时启动插件会拉起 managed 数据层，插件 stop 后 managed `8112` 退出；手动预先启动 `8112` 时插件识别为 external，插件 stop 后 external `8112` 仍存在。

- [x] **T-Live：live monitor summary tool** ✅ 已完成
  - 目标：真机测试时快速汇总 host / Hosted UI / data layer health、Hosted UI context、`/api/telemetry` 聚合状态、free-text dry_run-only 状态与逐源 blocked 摘要、replay 降级状态、T-Observe last decision/output 和日志异常计数。
  - 补充：Summary / Observe 摘要会保留 `kill_coalesced`，并在输出被压住、过期丢弃或重复折叠时显示 `output_backpressure` / `event_expired` / `repeated_event_collapsed`；`last_output_status` 会带 `event_age_seconds` / `event_expires_at` / `target_lanlan` / `battle_reply_contract` / `live_reply_contract` / `max_reply_chars` / `plugin_owned_output` / `dialogue_policy_owner=plugin` / `plugin_dialogue_policy` / `host_callback_contract_version`，用于判断多杀合并、输出背压、旧事件过期丢弃、重复折叠、fallback session、插件内短播报策略和通用 delivery contract 是否完整。
  - `tools/sample_replay.py` / `tools/offline_report.py` / `tools/live_test_plan.py` 三个出口与 `session_summary.next_steps` 都会列出 T-Output 背压与 T-Kill-Coalesce 多杀合并复测项，避免下一轮真机漏测这两个缓解延迟/刷屏的关键点。

- [x] **T-Output：output backpressure guard** ✅ 已完成
  - 目标：减少 `dry_run=false` 真机开口时旧事件晚回复和多条消息堆积。
  - 策略：真实 `push_message` 后，在 `output_backpressure_seconds` 窗口内压住同优先级或更低优先级事件；`you_killed`、`you_died` 和 critical 安全事件仍可通过，避免击杀夸夸被普通过载背压吃掉。窗口排队只保留最新观测事件的原始 `ts`，不在 flush 时刷新成当前时间；危急动作类默认走 bounded `respond` 并进入宿主 TTS，仅显式兼容开关允许插件短句 `blind+plugin` 直出；其他需要语气变化的事件同样走 bounded `respond`；真实战场事件携带 `coalesce_key=neko_warthunder:battle_event`；`output_event_max_age_seconds` 会在真实 push 前丢弃过期旧事件，同类安全提示短窗重复会以 `repeated_event_collapsed` 丢弃。真实输出还会携带 `event_age_seconds` / `event_expires_at` / `target_lanlan`、插件内短播报策略（`battle_reply_contract=short_tts_line` / `live_reply_contract=short_tts_line` / `max_reply_chars=28` / `dialogue_policy_owner=plugin` / `plugin_dialogue_policy` / `plugin_owned_output`）和通用 delivery-only `host_callback_contract.version=neko.callback.v1` 预留块。
  - 验收：新增 output backpressure / queue coalescing / event expiry / output freshness metadata / target session / plugin dialogue policy / generic delivery contract 合同测试，覆盖同/低优先级压制、高优先级通过、death/critical safety 绕过背压、dry_run 不受影响、窗口 flush 保留真实观测时间、低空旧 cue 可被死亡事件用相同 `coalesce_key` 替换、过期旧事件不进入真实 push、真实 push 暴露事件年龄与过期时间、event/context push 携带 `target_lanlan`，以及真实 battle event push 携带插件内短播报策略和 `host_callback_contract_version=neko.callback.v1`。
  - 后续：下一轮真机验证需要观察是否减少旧上下文回复，同时确认 critical / death 事件仍能插队，过期旧事件显示为 `event_expired`，`event_age_seconds` 合理，没有 fallback session 串路，且插件内 dialogue policy 和通用 delivery contract metadata 完整。不要继续为本插件扩宿主核心专用逻辑。

- [x] **T-Kill-Coalesce：you_killed 多杀合并** ✅ 已完成
  - 目标：减少连续 owned kill 导致的刷屏和主机队列堆积。
  - 策略：`you_killed` 在 `kill_coalesce_window_seconds` 窗口内合并为一条带 `kill_count` 的事件；`CRITICAL_RISK` 下 owned kill 不抢播、不丢弃，记录 `kill_deferred_critical_risk` 并在危急解除后按 `kill_coalesced` 补播；死亡 / critical 抢占仍会清空待播击杀。
  - 验收：新增 kill coalescing 合同测试，覆盖单杀延迟 flush、多杀合并、critical 抢占清空、`CRITICAL_RISK` 下延迟保留、危急解除后 flush、prompt 不含 unsafe raw。
  - 后续：下一轮真机重启后验证连杀场景下是否更少刷屏，确认 overspeed / low_alt / stall 等危急期间的击杀不会打断安全提醒，也不会在危急解除后丢失。
  - 边界：只读、不写同步磁盘日志、不记录 raw 玩家名、raw HUD、raw combat.feed、raw awards 文本。
  - 入口：`uv run python tools\live_monitor.py` 或 `uv run python tools\live_monitor.py --json`。
  - 已知数据层问题：`/api/telemetry.telemetry` 为空但 `processed.*` 可用；map/profile 轮询 `_merge_profile()` 参数缺失已修复为传入 family rules，并补回归测试；下次重启数据层后需确认日志不再刷。

- [~] **M3：适配 v1.6 DTO**
  - `overspeed`：已接 `processed.flags` 的 `overspeed_warn` / `overspeed_critical`，2026-06-23 真机 dry_run 已验证 warning/critical 事件链路。
  - `you_killed`：已监听 `combat.feed[]` 中 `is_my_kill == true` 的新 id。
  - `you_died`：已监听 `combat.feed[]` 中 `is_my_death == true` 的新 id；不把 `vehicle_valid` 跳变作为主路径。
  - `player_name`：插件侧已接 Hosted UI/context/action，通过 `/api/identity` 设置或清除权威身份；面板只支持手动填写/保存/清除玩家名，并持久化到插件配置。2026-06-23 真机已验证 `combat.self.source=manual` 与 `is_my_kill` / `is_my_death` owned 路径。`you_killed` 候选曾被 `SPAWNING` gate 压住，已修复为允许 owned combat kill，post-fix dry_run 与 `dry_run=false` push 已通过陆战验证。
  - `you_killed` / `you_died`：输出事实已按 `domain` / `cause` 使用空战 / 陆战 / 海战 / 坠毁 generic 文案，避免陆战击杀说成“击落坦克”，并避免 prompt 复读 raw victim 玩家名。
  - mode prompt：出场、战果、地面状态、空中态势和任务目标提醒均应带 `当前模式` 合同；陆战话术使用车组 / 上车 / 装填 / 掩体 / 看路，直升机话术使用机组 / 起飞 / 贴地 / 悬停 / 看高度，海战话术使用舰桥 / 上舰 / 出航 / 航向 / 海面。
  - `replay: true`：已在 DetectorEngine 静默并 reset，避免回放触发真实播报；T-Observe 会记录 `detector_suppressed/replay`，T-Live 会汇总 `replay_degrade.status` / `output_blocked`；仍需真实 replay 样本验证。
  - `overheat` / engine failure：`engine_overheat` / `oil_overheat` 已通过 `hud_notices` code 接入现有 `overheat`；2026-06-23 已观察到 `overheat` dry_run 基础链路；油温/发动机细项等待数据层数据库补齐后再校准，`powertrain_failure` 暂不直接播报，但会作为 `detector_suppressed/deferred_hud_notice` 进入 T-Observe / live monitor，后续再根据样本确定是否新增/映射故障事件。
  - `hud_notices` / `awards`：作为自由文本风险路径，正式播报前必须通过 T-Safety prompt 合同，并完成真机 dry_run 验证。

- [~] **L9：真机调参 / 真实开口终验**
  - 已完成：起飞/复活保护，避免机场滑跑或刚起飞阶段误播 `low_alt_danger`，并在 AGL 可用或起落架仍放下/运动中时避免滑跑阶段误播 `overspeed`。
  - 下一轮真机验证：记录 `radio_altitude_m` 是否可用；出生/机场起飞 45s 内不应低空播报；AGL 可用时 `<=10m` 进入贴地滑跑保护、`>=40m` 解除；AGL 不可用时，滑跑超速压制只应发生在保护期内且起落架放下/运动中；同窗口内失速、死亡仍应可触发；保护期后低空危险和真实离地超速恢复正常。
  - 继续观察：`dry_run=false` 下 T-Output、`event_expired`、`event_age_seconds`、`target_lanlan`、`battle_reply_contract`、`live_reply_contract`、`max_reply_chars`、`dialogue_policy_owner=plugin`、`plugin_dialogue_policy`、`host_callback_contract_version=neko.callback.v1` 是否减少旧事件晚回复、fallback session 和长回复，T-Kill-Coalesce 是否减少多杀刷屏。旧事件过期、短句和用户聊天干扰策略按插件内逻辑验收，不继续为本插件扩宿主核心专用逻辑。

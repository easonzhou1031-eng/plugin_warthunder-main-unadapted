# 真机测试结果：2026-06-23

## 范围

- 宿主后端、Hosted UI、数据层 `:8112`、War Thunder 真机环境。
- 覆盖 `dry_run=true` 与 `dry_run=false`。
- 同日追加空战 dry_run 监控覆盖 `:8111` 原生遥测、`:8112 /api/telemetry`、插件日志链路；该轮启动中 Hosted UI HTTP 端口未监听，因此只作为运行主链路证据，不重复计入 Hosted UI smoke。
- 本记录只保留聚合结论和安全摘要，不写 raw 玩家名、raw HUD 文本、raw combat.feed 原文或 awards 原文。

## 通过项

- Hosted UI context/action：可返回状态，`set_dry_run` / `test_say` 可调用。
- 数据层连接：插件可持续读取 `:8112 /api/telemetry`。
- 手动身份：`/api/identity` 设置后，数据层返回 `combat.self.source=manual`，并驱动 owned combat attribution。
- 空战数值安全：`spawn`、`low_fuel` warning/critical、`overheat` warning/critical、`stall_risk` warning/critical、`low_alt_danger` warning/critical 均进入 Arbiter / Dispatcher dry_run；重复命中可被 cooldown / scenario gate 解释。
- 空战追加 dry_run：`overspeed` warning/critical、`low_alt_danger` warning、`stall_risk` warning/critical 均被再次观察到；`overspeed` critical 可被 Arbiter `preempt`，重复命中可被 cooldown 解释。
- 空战 owned kill：`combat.feed` 出现 `is_my_kill=true`，证明 air ownership feed 可用。
- 空战 owned death：手动 identity 提前设置后，新 `combat.feed` 出现 `is_my_death=true`；追加监控中坠毁类 feed 也能生成 `you_died`，Arbiter allowed/preempt，Dispatcher dry_run 输出。
- 陆战 owned kill：`combat.feed` 出现多条 `is_my_kill=true`；插件生成 `you_killed`，Arbiter allowed，Dispatcher 输出。
- 陆战 owned death：`combat.feed` 出现 `is_my_death=true`；插件生成 `you_died`，Arbiter allowed，Dispatcher 输出。
- `dry_run=false`：`test_say`、`you_killed`、`you_died` 均被 `push_message` 接受；现场确认猫娘实际开口。
- T-Observe：`observe.last_event` / `last_decision` / `last_output_status` 能解释 allow、dry_run、pushed。

## 未见异常

- 未见 `PLUGIN_UI_ACTION_FAILED`。
- 未见后端 Traceback。
- 未见 TTS / push_message 报错。

## 仍需后续验证

- `replay=true` 样本验证。
- `awards` / hudmsg / 其他 free-text dry_run 安全合同。
- 油温 / 发动机细项等待数据层数据库或 profile 补齐后再校准。
- 数据层 map/profile 轮询 `_merge_profile()` 参数缺失报错需要数据层侧后续处理。
- 追加监控中 `:8112` fast telemetry、`processed.flags`、`combat.feed` 仍可用，但 map/profile 轮询持续刷 `_merge_profile()` 参数缺失，应由数据层侧确认是否影响地图/profile 相关功能。

## 收尾

- 本文件只记录脱敏聚合结论；现场 raw combat feed / 玩家名 / HUD 文本不写入 tracked 文档。

## 追加排障记录：状态上报与插件注册

- 2026-06-23 后续排障发现，插件过于频繁调用 `report_status()` 时可能触发宿主侧 ZMQ backpressure 日志刷屏；插件侧已改为状态快照去重与节流，状态变化仍会立即上报。
- 当日后续离线基线曾更新为 `151/151 passed`，pytest 为 `151 passed`；该数值是历史记录，当前基线以 `README.md` / `PROJECT_STATUS.md` 为准。新增 T-Output 输出背压和 T-Kill-Coalesce 多杀合并后，下一轮 `dry_run=false` 真机需复测旧事件晚回复 / 多条消息堆积是否改善。`tools/sample_replay.py` / `tools/offline_report.py` / `tools/live_test_plan.py` 三个出口与 `session_summary.next_steps` 都会列出这两个复测项，`tools/offline_report.py` 和 `tools/live_test_plan.py` 都会输出 Operator quick checklist；`tools/offline_report.py` 还会输出 Next test focus；`tools/live_monitor.py` 会在摘要中标出 `kill_coalesced` 与 `output_backpressure`，用于区分多杀合并和输出背压。
- 2026-06-25 复查发现，宿主侧 `N.E.K.O\plugin\plugins\neko_warthunder` 曾残留旧副本，导致运行时行为与独立仓库不一致。现已把该路径改为指向独立插件仓库的 junction；手动启动宿主时不再设置 `PLUGIN_CONFIG_ROOT` 指向外层工作区，避免重复扫出 `neko_warthunder_1`。复测确认 `/plugins/refresh`、`/plugin/neko_warthunder/start`、Hosted UI context/action 可用，且 `dry_run=true` 下 `test_say` 正确返回 `pushed=false, blocked="dry_run"`。

# 2026-06-20 样本回放报告

> 来源：合作者提供的真实 `/api/telemetry` 抓包样本，已本地留存在 `local_samples/data_process_20260620/`。样本目录被 `.gitignore` 排除，本报告只记录聚合统计和验证结论，不提交原始玩家名、HUD 文本、combat feed 原文或 awards 原文。

## 复现命令

```powershell
uv run python tools/sample_replay.py local_samples/data_process_20260620 tl0sr2
```

需要生成给现场测试用的操作清单时：

```powershell
uv run python tools/live_test_plan.py local_samples/data_process_20260620 tl0sr2
```

## 样本概况

- 输入文件：14 个
- telemetry 帧：10443
- 战斗态帧：`in_battle=8257`，`not_in_battle=2141`，`offline=45`
- domain 覆盖：`air=6606`，`ground=1593`，`menu=2186`，`naval=58`
- replay 覆盖：`replay_true=0`

## 已覆盖的安全事件

样本中出现并能通过插件离线 replay 产生 dry_run 输出的事件：

- `stall_risk`：warning / critical 均有候选与输出
- `low_alt_danger`：warning / critical 均有候选与输出
- `low_fuel`：warning 有输出，critical 有候选但被 Arbiter / 场景规则处理
- `overheat`：warning 有输出，critical 有候选
- `overspeed`：warning 有输出
- `spawn`：warning 有输出

这些结论说明：当前样本足以做数值安全主链路的离线回归，尤其能验证 Detector → Arbiter → Dispatcher dry_run 的基本链路。

## v1.6 DTO 覆盖结论

- `overspeed_warn=2`：已有超速 warning 样本。
- `overspeed_critical=0`：缺少超速 critical 样本，不能关闭 critical 档位验证。
- `combat_feed_items=2314`：combat feed 数量充足。
- `is_my_kill_field=0` / `is_my_death_field=0` / `involves_me_field=0`：当前样本没有 ownership 字段，不能关闭 kill/death 归属验证。
- `awards_items=1932`：awards 数据存在，但正式播报仍受 T-Safety 和真机 dry_run 验证阻塞。
- `proximity_events=5317` / `proximity_air_events=5300` / `proximity_rear_events=49` / `situation_frames=9970` / `situation_rear_air_threat_live_items=1906` / `ground_target_items=3253`：样本已覆盖 V2 空中接近、连续 situation 后方威胁与任务目标点数据。
- `enemy_on_six=149` / `tailing_risk=44`：合并 `records/*/proximity.jsonl*` 旁路流并消费 `situation.enemies` 后，后方 / 六点钟与持续尾随风险已能离线触发。当前样本虽有 ground target item，但未进入 `ground_target_nearby` 的 3000m 触发阈值。
- `combat_self_source=auto=5486, manual=1501`：样本包含 manual identity 帧，可用于回归 `/api/identity` 手动身份接缝。
- `hud_notice_codes=engine_overheat=414, flap_asymmetric=222`：已有 engine overheat notice。
- `oil_overheat=0`：缺少油温 notice 样本，不能关闭油温 notice 接缝验证。
- `powertrain_failure=0`：缺少动力故障 notice 样本，不能决定是否新增或映射故障事件。
- `hud_notice_severities=unknown=636`：notice severity 档位仍需真机确认。

## 当前 coverage_gaps

```text
no_replay_true_frames
no_overspeed_critical_flags
combat_feed_missing_ownership_fields
no_ground_target_close_candidates
no_ground_target_close_candidates
no_oil_overheat_notice_codes
no_powertrain_failure_notice_codes
hud_notice_severity_unknown
```

注：当前这包样本实际命中的是 `combat_feed_missing_ownership_fields`，即 ownership 字段完全缺失。`combat_feed_no_ownership_true_frames` 是后续样本可能出现的另一类缺口：字段存在，但没有任何 `is_my_kill` / `is_my_death` / `involves_me` 为 true 的帧。

## 当前 validation checks

| check | status | detail |
| --- | --- | --- |
| `free_text_safety` | `dry_run_only` | 样本中有 `awards` / `combat_feed` / `hud_notices`，但真实开口仍保持阻塞 |
| `numeric_safety` | `needs_more_samples` | 缺 `overspeed_critical` |
| `ownership` | `needs_more_samples` | 缺 v1.6 ownership 字段 |
| `proximity_awareness` | `needs_more_samples` | 已有空中接近、后方 / 六点钟、持续尾随、situation 与 ground target item；仍缺 3000m 内任务目标点候选 |
| `profile_calibration` | `needs_more_samples` | 缺 `oil_overheat` / `powertrain_failure` / notice severity 档位 |
| `replay_degrade` | `needs_more_samples` | 缺 `replay_true` |

## 对下一轮真机测试的影响

下次真机测试应优先补这些缺口；具体现场步骤可直接由 `tools/live_test_plan.py` 展开为“操作 / 监控 / 通过 / 失败 / 数据层缺口”清单：

1. 回放降级：确认 `/api/telemetry` 出现 `replay: true` 时插件静默 Detector 输出。
2. 超速 critical：触发或采集 `overspeed_critical`，验证优先级和 Arbiter 抢占语义。
3. kill/death ownership：通过 Hosted UI 设置玩家名后，确认 `combat.feed[].is_my_kill` / `combat.feed[].is_my_death` 出现并驱动 dry_run。
4. V2 接近/目标态势：捕获后方 / 六点钟 proximity 样本，验证 `enemy_on_six`；在对地任务中靠近到 3000m 内，验证 `ground_target_nearby` 的 Detector / Arbiter / Dispatcher dry_run 链路。
5. 油温 notice：采集 `hud_notices.feed[].code=oil_overheat`，确认映射到现有 `overheat`，且 raw 文本不进 prompt。
6. 动力故障 notice：采集 `powertrain_failure` 或等价字段后，再决定是否新增事件或继续不播。
7. notice severity：确认 warning / critical 档位是否稳定，不稳定则继续只用 safe code + 插件侧默认等级。
8. 输出背压：`dry_run=false` 下复测 `verify_output_backpressure`，确认同优先级或更低优先级旧事件不会继续排队晚回，高优先级事件仍可插队。
9. 多杀合并：复测 `verify_kill_coalescing`，确认连续 owned kill 能合成单条 generic 输出，且死亡 / 低空 / 失速仍能插队。

`tools/sample_replay.py`、`tools/offline_report.py`、`tools/live_test_plan.py` 三个出口与 `session_summary.next_steps` 都会列出上述 runtime output 复测项，避免统一测试时漏掉“旧回复晚到 / 多条堆积”的回归观察。

## 结论

这包样本可以支撑离线回归，但不能替代下一轮真机 dry_run。当前可以认为：

- 数值安全主链路离线回归可用。
- `/api/identity` manual 帧存在，适合继续做身份接缝回归。
- kill/death ownership、replay true、overspeed critical、V2 rear proximity、3000m 内任务目标点候选、oil overheat、powertrain failure 仍需要真机或新样本补齐。
- 自由文本相关路径继续受 T-Safety 约束，正式播报前必须先通过 dry_run。
- T-Output 背压与 T-Kill-Coalesce 多杀合并已进入下一轮现场复测清单，用于确认真实开口延迟和刷屏是否改善。

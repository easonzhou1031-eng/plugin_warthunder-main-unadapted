# 统一测试前离线检查

> 目标：在下一轮真机 / 数据层统一测试前，先确认当前插件仓库本身没有回归。本文只串联现有离线命令，不替代 `docs/真机验证-checklist.md`。

## 运行位置

从独立插件仓库 root 运行：

```powershell
cd D:\Users\zheng\Documents\Code\N-E-K-O-Warthunder\project-N-E-K-O-Warthunder-8111-data-plugin
```

可先用统一 helper 打印本轮离线门禁计划：

```powershell
uv run python tools\preflight.py
```

如果本地样本目录存在，计划输出会在 `local sample replay` 后提示查看 `session_summary`；这是统一测试前判断“已测到什么、下一步补什么”的入口。

确认路径无误后执行：

```powershell
uv run python tools\preflight.py --run
```

需要把离线 readiness report 一起保存给协作者或后续接手者时：

```powershell
uv run python tools\preflight.py --run --report-output local_samples\offline-readiness.md
```

下面保留的是逐条手动命令，便于单项重跑或排障。

## 1. 逻辑与 pytest 基线

```powershell
uv run python tests/run_logic_tests.py
uv run pytest -c tests\pytest.ini tests -q
```

预期：

- `579/579 passed`
- pytest 通过（数量以当前测试收集结果为准）

如果数量变化，以 `README.md` 的当前基线为准；不要沿用旧的 29/32/42/71/74/78/127/281/286/300/304/312 结果。

## 1.1 free-text release gate

```powershell
uv run python tools\free_text_gate.py
uv run python tools\free_text_gate.py --json
uv run python tools\release_defaults_gate.py
uv run python tools\release_defaults_gate.py --json
```

预期：

- `status: pass`
- `policy.hudmsg_combat_feed_awards_real_output_allowed=false`
- 合成玩家名、HUD、combat.feed、awards raw 文本不出现在 prompt、`push_message.parts[].text` 或 safe payload 中。
- `release_defaults_gate` 应显示 `dry_run_first=true`、`v2_live_verified_real_output_enabled=false`、`debug_timeline_enabled=false`，并确认输出背压 / 事件过期 / 多杀合并等默认保护仍开启。

该门禁不替代真机 dry_run，但它是 hudmsg / combat.feed / awards 去桩前的离线硬检查。

## 1.2 replay degrade gate

```powershell
uv run python tools\replay_gate.py
uv run python tools\replay_gate.py --json
```

预期：

- `status: pass`
- `summary.candidates=0`
- `summary.prompts=0`
- `summary.push_messages=0`

该门禁不替代真实 replay 样本，但它是 `replay=true` 降级安全的离线硬检查。

## 1.3 deferred HUD notice gate

```powershell
uv run python tools\deferred_hud_gate.py
uv run python tools\deferred_hud_gate.py --json
```

预期：

- `status: pass`
- `summary.deferred_notice=powertrain_failure`
- `summary.deferred_candidates=0`
- `summary.prompt_built_for_deferred_notice=false`
- `summary.push_called_for_deferred_notice=false`

该门禁不决定是否新增动力故障播报事件；它只锁定当前策略：有真实样本 / profile 校准前，先不播。

## 1.4 mode / domain boundary gate

```powershell
uv run python tools\domain_boundary_gate.py
```

预期：

- `status=pass`
- fixed-wing condition cues stay air-only: `stall_risk`、`high_aoa`、`over_g`、`low_alt_danger`、`overspeed`、`low_fuel` 不应在 ground / naval / heli / unknown domain 触发。
- ground speech only keeps `ground_laser_warning`，且不应在 air / heli / naval / unknown domain 触发；乘员、岗位和弹药 flags 不应生成播报候选。

这个门禁用于防止后续新增事件时重新把陆战/海战/直升机误接到固定翼飞控逻辑上。

开发者还应同步检查输出层合同：

- `tests/test_dispatcher_safety.py` 中的 spawn / kill / ground-status prompt 测试应覆盖 `当前模式`。
- `push_message.metadata.domain_prompt_contract` 应与 prompt 中的模式一致。
- 陆战 prompt 不应出现 `升空`、`后座`、`云霄`、`机翼`、`拉杆` 等固定翼语境。
- 直升机 prompt 不应自动猜固定翼后座 / 机翼动作。
- unknown domain 只能泛化招呼，不猜载具类型。

## 1.5 proximity / objective awareness gate

```powershell
uv run python tools\proximity_gate.py
uv run python tools\v2_readiness.py --no-sample
uv run python tools\v2_release_matrix.py --no-sample
uv run python tools\v2_output_policy_gate.py
uv run python tools\v2_completion_gate.py --no-sample
uv run python tools\rc_handoff_report.py --no-sample
uv run python tools\final_smoke_packet.py
```

预期：

- `status=pass`
- V2 事件 `enemy_nearby`、`air_threat_nearby`、`enemy_on_six`、`tailing_risk` 能由合成 `proximity.events` 产生，`ground_target_nearby` 能由合成 `situation.ground_targets` 产生。
- `COMBAT_STRESS` 下普通接近和任务目标点被低优先级门控压住，critical safety 事件仍然优先。
- prompt / `push_message.parts[].text` 不包含 raw proximity 文本、raw objective 文本或目标 label。
- `tools\v2_readiness.py --no-sample` 输出 `v2_code_complete=true`、`v2_offline_gate_complete=true`、`v2_live_evidence_complete=false`，表示代码与离线门禁完成，但真机样本证据仍按 checklist 补齐。
- `tools\v2_release_matrix.py --no-sample` 输出 `verdict=v2_code_complete_live_pending`，并按能力列出 code/offline/live-evidence/real-output-policy；没有真机证据的后方威胁、持续尾随和地面目标提醒保持 dry_run-first。
- `tools\v2_output_policy_gate.py` 输出 `status: pass`，并证明 `enemy_on_six`、`tailing_risk`、`ground_target_nearby` 在 `v2_live_verified_real_output_enabled=false` 时不会真实 `push_message`，但 dry_run 仍可观察；显式开启后才允许真实推送。
- `tools\v2_completion_gate.py --no-sample` 输出 `verdict=v2_code_offline_complete_live_evidence_pending`，表示 V2 大目标的代码/离线/输出策略已收口，但仍不能把缺失的真机样本说成已验证。
- `tools\rc_handoff_report.py --no-sample` 输出 `ready_for_rc_handoff_report`，用人类可读方式汇总 V1 release scope、V2 completion、final smoke go/no-go、安全边界和 remaining live actions。
- `tools\final_smoke_packet.py` 默认输出 `go_no_go=review_required_run_offline_gate`，表示先跑离线门禁；`release_readiness.py --run` 通过后再用 `tools\final_smoke_packet.py --offline-gates-passed` 输出 `go_no_go=go_dry_run_final_smoke`。交接包会列出必跑命令、V2 逐能力 live evidence 矩阵、observed/triggered 计数、runtime focus checks、remaining live actions 和 `raw_text_printed=false` 安全边界。跑真机前先用 `tools\final_smoke_evidence_gate.py --safe-transcript-template --output local_test_logs\safe_transcript_metrics.json` 生成猫猫实际回复 metrics 模板；跑完真机后保存 `tools\live_monitor.py --count 3 --interval 1 --json --output local_test_logs\live_monitor_final.json`，再用 `tools\final_smoke_evidence_gate.py --from-live-monitor local_test_logs\live_monitor_final.json --output local_test_logs\final_smoke_evidence.json` 扫描整段 JSONL 并预填 evidence 草稿；填好无原文 metrics 后优先用 `tools\final_smoke_evidence_gate.py local_test_logs\final_smoke_evidence.json --safe-transcript local_test_logs\safe_transcript_metrics.json --confirm-mode-domain-boundary` 合并猫猫行数、字数、是否续写、聊天静默、critical 替换观察和 mode/domain 边界确认；没有 metrics 时再用 `tools\final_smoke_evidence_gate.py local_test_logs\final_smoke_evidence.json --update --confirm-critical-replaced-stale-warning --confirm-user-chat-quiet-window --confirm-short-tts-single-line --confirm-mode-domain-boundary` 合并确认；最终用 `tools\final_smoke_evidence_gate.py local_test_logs\final_smoke_evidence.json` 验收 P1 evidence，也可以在统一入口追加 `--final-smoke-evidence local_test_logs\final_smoke_evidence.json`。
- 推荐使用交接包里的 `evidence_from_monitor_and_transcript` 一步合成：`tools\final_smoke_evidence_gate.py --from-live-monitor local_test_logs\live_monitor_final.json --safe-transcript local_test_logs\safe_transcript_metrics.json --confirm-mode-domain-boundary --output local_test_logs\final_smoke_evidence.json`。分步的 `--from-live-monitor` 和 `--safe-transcript` 命令保留为排障/补录路径。
- 推荐用交接包里的 `safe_transcript_record` 生成 metrics：`tools\final_smoke_evidence_gate.py --record-safe-transcript --reply-chars <count> --reply-lines 1 --confirm-critical-replaced-stale-warning --confirm-user-chat-quiet-window --output local_test_logs\safe_transcript_metrics.json`。该命令只记录数字和确认 flag，不保存回复原文。
- 推荐先跑交接包里的 `evidence_rehearsal`：`tools\final_smoke_evidence_gate.py --rehearsal-output-dir local_test_logs\final_smoke_rehearsal`。该输出带 `rehearsal_only=true`，只证明证据流程，不替代真机 evidence。

## 1.6 release readiness

```powershell
uv run python tools\release_readiness.py
uv run python tools\release_readiness.py --run
```

预期：

- 计划模式显示 logic tests、pytest、vehicle profile id audit、release defaults gate、output freshness gate、host boundary gate、free-text release gate、replay degrade gate、ownership replay gate、deferred HUD notice gate、proximity/objective awareness gate、V2 readiness summary、V2 release matrix、V2 output policy gate、V2 completion gate、RC handoff report、final smoke packet、synthetic replay、宿主存在时的 host boundary gate 和 plugin check。
- `--run` 全通过时返回 `ready_for_final_live_smoke`。

默认 `--run` 是快门禁，不会因为本机存在 ignored 的 `local_samples/` 大样本而自动跑 `sample_replay` / `offline_report` / `live_test_plan`。需要把本地样本证据也纳入同一轮汇总时再显式运行：

```powershell
uv run python tools\release_readiness.py --run --include-local-sample
```

该入口不启动前后端，不依赖 War Thunder；通过后再进入最后一轮聚焦真机 smoke。

## 1.7 host callback contract reservation

插件侧已经预留未来宿主通用接口，不需要继续为本插件扩宿主核心专用逻辑。单项检查：

```powershell
uv run python tools\output_freshness_gate.py
uv run python tools\output_freshness_gate.py --json
```

预期：

- `status: pass`
- `host_callback_contract_version=neko.callback.v1`
- metadata 中存在结构化 `host_callback_contract`
- `delivery.replace_pending=true`
- urgent / death 类事件带 `delivery.interrupt_pending=true`
- `dialogue_policy_owner=plugin`
- `plugin_dialogue_policy.mode=short_tts_line`
- `plugin_dialogue_policy.max_chars=28`
- `plugin_quiet_window_policy=suppress_non_urgent_during_user_input`

`host_callback_contract` 只声明通用投递/新鲜度语义；短句裁剪、旧事件过期和用户聊天干扰策略收在插件 metadata 与插件逻辑里，不应在核心层写 `neko_warthunder` 特判。

## 2. 插件注册检查

从宿主仓库运行：

```powershell
cd D:\Users\zheng\Documents\Code\N-E-K-O-Warthunder\N.E.K.O
uv run python -m plugin.neko_plugin_cli.cli check D:\Users\zheng\Documents\Code\N-E-K-O-Warthunder\project-N-E-K-O-Warthunder-8111-data-plugin
```

预期：

- `0 error(s)`
- 允许已有固定 warning：目录名与 plugin id 不同、缺 `.vscode` / GitHub workflow。

## 2.1 宿主运行时注册注意

当前工作区里 `N.E.K.O\plugin\plugins\neko_warthunder` 必须是指向独立插件仓库的 junction。手动启动宿主时不要额外设置 `PLUGIN_CONFIG_ROOT` 指向外层工作区；否则宿主可能同时扫到 junction 和独立仓库目录，出现重复插件 ID 或加载旧副本。建议使用：

```powershell
cd D:\Users\zheng\Documents\Code\N-E-K-O-Warthunder\N.E.K.O
uv run python launcher.py
```

启动后确认注册：

```powershell
Invoke-RestMethod http://127.0.0.1:48916/plugins
Invoke-RestMethod -Method Post http://127.0.0.1:48916/plugins/refresh
Invoke-RestMethod -Method Post http://127.0.0.1:48916/plugin/neko_warthunder/start
```

若 refresh 报出外层独立仓库目录名的重复/无效项、出现 `neko_warthunder_1`，或 `test_say` 在 `dry_run=true` 下仍返回 `pushed=true`，不要继续测试；先修正 junction / 启动环境，确保宿主加载的是独立仓库最新代码。

## 3. 合成 replay 演示

```powershell
uv run python tools\replay.py
```

预期输出中应至少出现：

- `spawn/warning`
- `stall_risk/critical`
- `low_fuel/warning`
- `you_killed/warning`
- `you_died/critical`
- `battle_end/warning`

这只证明离线合成场景链路正常，不证明真机 ownership、TTS 或数据层字段稳定。

## 4. 真实样本离线 replay

如果本机有合作者样本目录：

```powershell
uv run python tools\sample_replay.py local_samples\data_process_20260620 tl0sr2
```

需要机器可读结果时：

```powershell
uv run python tools\sample_replay.py local_samples\data_process_20260620 tl0sr2 --json
```

需要机器可读 RC gap summary 时：

```powershell
uv run python tools\rc_gap_summary.py local_samples\data_process_20260620 tl0sr2
uv run python tools\rc_gap_summary.py local_samples\data_process_20260620 tl0sr2 --json
```

重点看：

- `sample_unproven_items`：当前本地样本还不能证明的轨道。
- `blocked_release_items`：当前策略下仍不允许真实发布/真实播报的轨道，例如 free-text real output。
- `remaining_gaps` / `next_actions`：下一轮真机或样本要补的最小集合。

需要生成可交付 Markdown 汇报时：

```powershell
uv run python tools\offline_report.py local_samples\data_process_20260620 tl0sr2
```

需要机器可读的安全汇总时：

```powershell
uv run python tools\offline_report.py local_samples\data_process_20260620 tl0sr2 --json
```

需要写入文件时：

```powershell
uv run python tools\offline_report.py local_samples\data_process_20260620 tl0sr2 --output local_samples\offline-readiness.md
```

Markdown 报告会包含 Team brief、Operator quick checklist 和 Next live-test plan；JSON 也会包含 `quick_checklist`；输出目录不存在时会自动创建。

需要直接生成下一轮真机操作清单时：

```powershell
uv run python tools\live_test_plan.py local_samples\data_process_20260620 tl0sr2
```

该清单会先输出 Operator quick checklist，再把 P1/P2 项展开成“操作 / 监控 / 通过 / 失败 / 数据层缺口”。`sample_replay` / `offline_report` / `live_test_plan` 三个出口与 `session_summary.next_steps` 都会列出 T-Output 背压与 T-Kill-Coalesce 多杀合并复测项；`offline_report` 还会输出 V2 capability evidence，方便测试前确认每项 V2 能力到底是 `covered_by_current_sample`、`needs_live_sample` 还是 `not_checked`。

真机测试进行中需要快速看当前链路状态时：

```powershell
uv run python tools\live_monitor.py
uv run python tools\live_monitor.py --json
```

该工具只做只读检查：health、Hosted UI context、`/api/telemetry` 聚合计数、free-text dry_run-only 状态、replay 降级状态、T-Observe last decision/output 和最近日志异常计数。输出不得包含 raw 玩家名、raw HUD 文本、raw combat.feed 或 awards 原文。若现场出现自由文本源，应看到 `free_text=dry_run_only(...)`、`FreeText detail: awards=.../blocked` 等逐源摘要；插件运行态还会在首次看到 `awards` / `combat.feed` / `hud_notices` / `hudmsg` / `hud_events` 时给出 `detector_suppressed/free_text_blocked`，并在 dry_run 下用 `free_text_activity` 走完 Detector / Arbiter / Dispatcher 可观测链路；`dry_run=false` 必须仍以 `free_text_dry_run_only` 阻断真实输出。若现场出现 `replay=true`，应看到 `replay=suppressed(detector_suppressed/replay)` 和 `output_blocked=True`。

当前样本预期：

- `frames: 10443`
- `combat_self_source=auto=5486, manual=1501`
- `awards_items=1932`
- `coverage_gaps` 包含：
  - `no_replay_true_frames`
  - `no_overspeed_critical_flags`
  - `combat_feed_missing_ownership_fields`
  - `no_rear_threat_candidates`
  - `no_ground_target_close_candidates`
  - `no_oil_overheat_notice_codes`
  - `no_powertrain_failure_notice_codes`
  - `hud_notice_severity_unknown`

如果拿到新样本，重点看 `coverage_gaps` 是否减少。特别是：

- `session_summary` / offline readiness report：快速看已观察事件、已输出 dry_run 事件、分组 validation verdict、Operator quick checklist、P1/P2 `live_test_plan`、剩余真机范围和建议的下一步补测项。

- `combat_feed_missing_ownership_fields`：完全没有 ownership 字段。
- `combat_feed_no_ownership_true_frames`：字段存在，但没有命中我方击杀/死亡。
- `no_manual_identity_frames`：没有手动 `/api/identity` 生效帧。

## 5. 真机测试前结论

离线检查全过后，仍不能关闭这些项：

- `replay: true` 真实样本验证。
- `overspeed_critical` 真机触发 / 样本验证。
- 设置玩家名后的 `is_my_kill` / `is_my_death` ownership true 验证。
- `oil_overheat` notice 验证。
- `powertrain_failure` notice 策略判断。
- `dry_run=false` 真实开口终验。

这些继续按 `docs/真机验证-checklist.md` 执行。
